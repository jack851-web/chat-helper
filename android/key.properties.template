# 复制为 android/key.properties 并填入真实签名（不要提交到 Git）
# 示例：
# storeFile=/Users/you/keys/chat-helper-release.jks
# storePassword=your-store-password
# keyAlias=chat-helper
# keyPassword=your-key-password
storeFile=
storePassword=
keyAlias=
keyPassword=
