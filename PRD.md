# Chat-Helper 产品需求文档（PRD）

> **版本**：v2.5（UI深度优化版）
> **状态**：需求评审通过 / 开发中
> **更新**：2026-06-07
> **核心变更（相对 v2.4）**：状态同步修复 + 进度阶段提示 + 联系人标签 + 草稿入口增强 + 悬浮球保活 + JSON容错升级 + 常量统一 + 协议版本协商 + 全面UI体验优化
> **v2.4 变更**（第三轮深度优化）：
> - 设置页版本号同步至v2.3，消除用户困惑
> - 死代码彻底清除：移除Dart端handleFloatingBallClick/captureScreenshot/_handleFloatingBallClick/_mapError及main.dart旧入口
> - 网络预检：截图完成后AI调用前先检测网络连通性（DNS查询火山引擎域名，3秒超时），无网立即提示避免等待180秒
> - 异常分类处理：catch块细分为SocketException（网络）、TimeoutException（超时）、FormatException（数据格式）、通用异常四类，错误提示精准化
> - 记忆列表统一使用getAllMemories：与DB层220条上限策略保持一致
> - 换一批浮窗内加载反馈：点击刷新按钮后禁用按钮+标题变"正在生成中..."，不再立即关闭浮窗
> - 清空记忆防误触升级：红色边框OutlinedButton + 危险操作确认对话框 + 红色确认按钮
> - 启动IO并行化：recoverStuckProcessingRecords与cleanupOldScreenshots通过Future.wait并行执行
> - 联系人列表防抖从30秒缩短至5秒，编辑联系人后更快看到更新
> - db封装增强：新增getExistingSuggestionContents封装方法，platform_service不再直接执行原始SQL
> - Java端mkdirs返回值检查：目录创建失败时Toast提示+球状态置ERROR，防止静默失败
> **v2.5 变更**（第四轮深度优化 — 状态同步 + UI体验）：
> - **状态同步修复**：解决"返回App后球立刻变空闲但任务还在处理"的核心问题——Java端新增 `dartIsProcessing` 状态标志，Dart 端 `_isProcessing` 全生命周期追踪，`onResume` 时自动恢复球的 processing 视觉状态
> - **进度阶段提示**：悬浮球横条文字从固定"识别中..."升级为三阶段动态提示：「识别中...」→「识别对话中...」→「生成建议中...」，用户全程感知处理进度
> - **联系人标签**：悬浮球旁新增联系人名字小标签（半透明圆角矩形），切换联系人时实时同步显示，用户在任何界面都知道"现在帮谁聊天"
> - **草稿入口增强**：建议浮窗新增"存入草稿"按钮（书签图标），点击后通知Dart端保存当前建议到草稿箱，解决草稿功能形同虚设的问题
> - **悬浮球保活/自恢复**：App被杀重启或系统回收后，`onResume` 自动检测并重建悬浮球（含联系人标签），无需用户手动打开App
> - **JSON修复性解析升级**：从2级修复扩展为6级容错——Markdown提取→边界定位→尾逗号修复→截断补全→部分字段提取兜底，AI返回格式异常时不再整体丢弃
> - **常量统一配置中心**：将散落在 AppConstants、VisionService、PlatformService、Java端的关键常量统一收敛到 `AppConstants`（protocolVersion/channelName/timeout/screenshotQuality/draftLimit等）
> - **MethodChannel协议版本协商**：新增 `PROTOCOL_VERSION` 常量 + `getProtocolVersion` 方法，启动时自动检测 Dart/Java 两端版本兼容性，不匹配时优雅降级
> - **UI全面优化计划**（详见 §15 UI优化路线图）：Tab动画、联系人搜索、头像差异化着色、气泡长按菜单、日期分组、骨架屏、首次引导等

---

## 目录

1. [产品概述](#1-产品概述)
2. [目标用户与典型场景](#2-目标用户与典型场景)
3. [单AI引擎架构](#3-单ai引擎架构)
4. [功能需求清单与优先级](#4-功能需求清单与优先级)
5. [功能详述](#5-功能详述)
   - [5.1 截图获取](#51-截图获取)
   - [5.2 悬浮球状态反馈](#52-悬浮球状态反馈)
   - [5.3 全局浮窗建议卡片](#53-全局浮窗建议卡片)
   - [5.4 豆包单次多任务调用（合并 AI-1 + AI-2）](#54-豆包单次多任务调用合并-ai-1--ai-2)
   - [5.5 联系人档案管理](#55-联系人档案管理)
   - [5.6 话术草稿编辑器](#56-话术草稿编辑器)
   - [5.7 历史对话记录](#57-历史对话记录)
   - [5.8 设置与参数配置](#58-设置与参数配置)
6. [交互流程总览](#6-交互流程总览)
7. [UI 设计方向](#7-ui-设计方向)
8. [数据模型](#8-数据模型)
9. [技术可行性评估](#9-技术可行性评估)
10. [API 集成规范（V2 豆包单引擎）](#10-api-集成规范v2-豆包单引擎)
11. [上下文注入规范（V2 重点）](#11-上下文注入规范v2-重点)
12. [增量去重与落库策略](#12-增量去重与落库策略)
13. [关键评审事项与建议](#13-关键评审事项与建议提交开发前必看)
14. [未来路线图（v2.3+）](#14-未来路线图v23)
15. [UI 体验优化路线图（v2.5）](#15-ui-体验优化路线图v25)

---

## 1. 产品概述

### 1.1 一句话定位

**Chat-Helper** 是一款"常驻桌面、一键截图、AI 参谋"的智能聊天辅助工具——点一下悬浮球，**豆包**在一次调用中完成对话提取 + 回复建议生成，直接浮在聊天 App 上方，看一眼就知道怎么回。

### 1.2 核心价值主张

| 痛点 | 解决方案 |
|------|----------|
| 聊天冷场、不知道回什么 | AI 先给方向思路，再给具体话术 |
| 回复像机器人、不自然 | Prompt 三重强调"说人话"，禁止 AI 腔 |
| 切换 App 麻烦 | 点击悬浮球→浮窗直接出现在聊天App上方，无需切回 |
| 点了不知道有没有反应 | 悬浮球即时动画反馈（处理中→成功/失败） |
| 多联系人记不住上下文 | 每位联系人独立记忆库，AI 单次调用同时取最近6条+全量历史 |
| 等待时间太长 | V2 单AI 调用，延时从 5-14s 缩短到 **3-6s** |

### 1.3 V2 核心变更点（vs v1.5）

| 项目 | v1.5（双AI） | v2.0（单AI） |
|------|-------------|--------------|
| AI 引擎数 | 2（豆包+DeepSeek） | **1（豆包）** |
| API 调用次数 | 2 | **1** |
| 总延时 P95 | 5-14s | **3-6s** |
| 单次 token | 豆包 ~2k + DeepSeek ~3k | **豆包 ~4-5k** |
| 联系人档案注入 | AI-2 注入 | **豆包一次性注入** |
| 方向铁律 | AI-2 prompt | **豆包 prompt** |
| 落库策略 | 豆包落对话，DeepSeek 落建议 | **豆包一次性落对话+建议** |
| 设置页 | AI-1 + AI-2 双区 | **仅豆包配置区** |
| API Key 数 | 2（豆包+DeepSeek） | **1（豆包）** |

---

## 2. 目标用户与典型场景

### 2.1 核心场景

**场景**：微信收到消息，不知如何回应
1. 打开微信看到消息
2. 点击悬浮球 → 悬浮球立即变为"处理中"旋转动画
3. 几秒后，微信界面上方弹出一张浮窗卡片——"对话思路"+3条具体话术
4. 看思路、选话术 → 点复制 → 贴回微信 → 浮窗自动消失
5. **全程不需要切回 Chat-Helper App**

---

## 3. 单AI引擎架构

### 3.1 架构图（V2 单AI 多任务）

```
┌──────────────────────────────────────────────────┐
│                  用户点击悬浮球                     │
│          悬浮球立即变为"处理中"旋转动画               │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│           截图：AccessibilityService               │
│           takeScreenshot() 无感截图                │
│           不弹任何对话框                            │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│  加载当前联系人：                                    │
│   • 最近 6 条记忆（用于增量去重判断）                │
│   • 全量历史记忆（用于建议生成参考，头尾保留策略：前20条+最近200条，硬上限300条）    │
│   • 联系人档案（语气/长度/创意度）                   │
│   • 当前时间戳                                      │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│      豆包单次多任务调用（云端，1次 HTTP）           │
│  ┌────────────────────────────────────────────┐  │
│  │ System Prompt                                │  │
│  │  ├ 角色铁律（防回复角色反转）                  │  │
│  │  ├ 增量去重规则（基于最近6条）                │  │
│  │  ├ 三大场景判断（A/B/C）                     │  │
│  │  ├ "说人话"三重强调                          │  │
│  │  ├ 输出 JSON Schema 约束                      │  │
│  │  └ Few-shot 示例（2-3条）                     │  │
│  ├────────────────────────────────────────────┤  │
│  │ User Input（按端点格式）                      │  │
│  │  ├ 【上下文文本】                             │  │
│  │  │   ├ 联系人档案                            │  │
│  │  │   ├ 当前时间                              │  │
│  │  │   ├ 全量历史记忆（[self]/[other] 标记）    │  │
│  │  │   └ 最近 6 条记忆（单独 section）          │  │
│  │  ├ 截图 base64                                │  │
│  │  └ 任务指令（"识别新增 + 生成建议"）            │  │
│  └────────────────────────────────────────────┘  │
│                                                     │
│  输出（按 §5.4.3 Schema）：                          │
│   • new_messages[] （新增对话）                      │
│   • scene + scene_description                       │
│   • contact_insight（基于全量历史的画像摘要）         │
│   • direction（对话方向）                            │
│   • suggestions[3]（3条具体话术）                    │
│                                                     │
│  Base URL：ark.cn-beijing.volces.com/api/v3          │
│  端点：/responses（Seed系列）或 /chat/completions     │
│  默认模型：doubao-seed-2-0-mini-260428             │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│      客户端解析 + 落库                                │
│   • new_messages → 落 chat_memories（幂等校验）      │
│   • suggestions → 落 suggestions 表                  │
│   • 失败时浮窗显示 Toast，不阻塞                     │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│   全局浮窗卡片弹出（WindowManager，聊天App上方）     │
│   场景标签 + 💡对话思路 + 联系人画像 + 3条具体话术    │
│   悬浮球绿色闪光 → 恢复空闲 → 浮窗N秒后自动消失      │
└──────────────────────────────────────────────────┘
```

### 3.2 AI 引擎总览

| 维度 | 豆包（V2 唯一引擎） |
|------|-------------------|
| **提供商** | 火山引擎 豆包 Seed Vision |
| **API** | `POST /responses`（Seed系列）或 `POST /chat/completions`（1.5 Vision Pro） |
| **Base URL** | `https://ark.cn-beijing.volces.com/api/v3` |
| **推荐模型** | `doubao-seed-2-0-mini-260428` |
| **图片上传** | 是（截图 base64） |
| **任务数** | **2**（对话提取 + 建议生成，一次调用） |
| **延迟** | 3-6s（含网络+推理） |
| **状态** | 无状态（客户端自行拼装完整 prompt） |

### 3.3 上下文注入分层（V2 关键设计）

为了一次调用中让豆包完成两件事，**上下文按用途分层注入**：

| 注入层 | 内容 | 用途 | 体积 |
|--------|------|------|------|
| **L1** 联系人档案 | name/relationship/tone/length/creativity | 个性化输出 | ~100 token |
| **L2** 全量历史 | 所有 `chat_memories`（**头尾保留策略：前20条 + 最近200条，硬上限300条**） | 理解人物与背景 | **~100-5000 token**（大头） |
| **L3** 最近 6 条 | 末尾 6 条 | **增量去重判断基准** | ~150 token |
| **L4** 当前截图 | base64 | 视觉识别 | 500-2000 token |
| **L5** 当前时间 | ISO8601 | 时间上下文 | ~30 token |
| **L6** 任务指令 | 增量去重 + 建议生成 + 输出schema | 引导模型行为 | ~200 token |

**总 token 估算**：~2000-8000（豆包 16k 上下文够用）

---

## 4. 功能需求清单与优先级

### 4.1 优先级定义

| 级别 | 含义 |
|------|------|
| **P0** | 核心必做，MVP必须上线 |
| **P1** | 重要功能，早期版本上线 |
| **P2** | 锦上添花，迭代中加入 |

### 4.2 功能全景

| 模块 | 功能点 | 优先级 | 备注 |
|------|--------|--------|------|
| **截图** | AccessibilityService 无感截图 | P0 | |
| **悬浮球反馈** | 状态动画（处理中/成功/失败） | P0 | |
| | 防重复点击保护 | P0 | |
| **AI 引擎（豆包）** | 截图对话提取（视觉识别） | P0 | |
| | 增量去重（最近6条记忆比对） | P0 | |
| | 时间戳输出 | P0 | |
| | 三大场景自动判断（A/B/C） | P0 | |
| | 对话方向(direction) + 3条建议 | P0 | |
| | 方向铁律（防回复角色反转） | P0 | |
| | 联系人画像摘要（contact_insight） | P1 | 基于全量历史 |
| | 时间上下文感知（时间戳+间隔） | P0 | |
| | 提示词强调"说人话" | P0 | |
| | 提取结果自动落库（仅新增） | P0 | |
| | 建议落库 | P0 | |
| | 每条建议附带理由 | P1 | |
| | 换一批（重新生成） | P1 | |
| **浮窗卡片** | 全局浮窗（聊天App上方弹出） | P0 | |
| | 自动消失（可设置时间） | P0 | |
| | 一键复制 | P0 | |
| | 复制后延期消失 | P1 | |
| **联系人** | 新建/编辑/删除 | P0 | |
| | 列表/搜索/快速切换 | P0 | |
| **记忆库** | 对话自动保存 | P0 | |
| | 历史时间线查看 | P0 | |
| | 关键词搜索 | P1 | |
| **设置** | 豆包 API Key | P0 | |
| | 豆包模型选择（含自定义） | P0 | 自动判断端点 |
| | 浮窗显示时间 | P0 | 5s/8s/15s/手动 |
| | 深色模式 | P1 | |

---

## 5. 功能详述

### 5.1 截图获取

Android `AccessibilityService.takeScreenshot()`（Android 11+）。

> **不弹"录制屏幕"对话框。** 首次去"设置→无障碍"开启服务后，之后完全无感。

#### 5.1.1 截图前置校验（v2.2 新增）

截图前必须检查联系人选择状态，**未选联系人时拦截，不执行截图**：

```
用户点击悬浮球
    │
    ▼
检查当前是否已选联系人？
    │
    ├── 未选 → 悬浮球显示红色叉号 + Toast "请先选择联系人"
    │          不截图、不调用AI
    │
    └── 已选 → 继续截图流程（AccessibilityService）
```

> **设计理由**：未选联系人时如果允许截图，数据会存到不可见的 `_default` 占位联系人下，导致用户切换到正式联系人后看不到之前的记录。前置拦截可避免无效截图浪费和"数据丢失"。

### 5.2 悬浮球状态反馈

点击悬浮球后必须有即时反馈，用户不能疑惑"到底点没点上"。

#### 5.2.1 状态定义

```
  空闲              处理中（横条）          成功              失败
 ┌─────┐          ┌──────────────────┐   ┌─────────┐      ┌─────────┐
 │ 💬  │ 点击→    │ ⟳ 识别中...      │   │ ✓  💬  │      │ ✗  💬  │
 │     │          │ (横向加载条)       │   │ 绿色闪光  │      │ 红色闪烁  │
 └─────┘          └──────────────────┘   └─────────┘      └─────────┘
 48dp圆形           160dp×40dp 横条        3秒后回空闲       3秒后回空闲
 可点击             以原球心居中展开         不可点击           可点击
 可拖拽             不可点击/拖拽            可拖拽             可拖拽

 联系人标签（v2.5 新增）：
 ┌──────────┐
 │ 小明     │ ← 半透明圆角标签，悬浮在球上方/下方
 └──────────┘
```

**进度阶段提示（v2.5 新增）**：处理中状态不再固定显示"识别中..."，而是根据 AI 流水线阶段动态更新：

| 阶段 | 横条文字 | 触发时机 |
|------|---------|----------|
| **processing** | `识别中...` | 点击悬浮球后，截图完成前 |
| **analyzing** | `识别对话中...` | 截图完成，豆包正在分析对话内容 |
| **generating** | `生成建议中...` | 对话已提取，正在生成回复建议 |

**状态转换动画细节：**

| 转换 | 动画效果 |
|------|---------|
| **圆形 → 横条（处理中）** | 记录球心坐标 → 隐藏圆形图标 → 横条从 `scaleX=0.3, alpha=0` 弹出至正常大小（280ms，DecelerateInterpolator）→ 横条位置以原球心水平居中对齐 |
| **横条 → 圆形（成功/失败）** | 隐藏横条 → 显示圆形图标 → 图标从 `scale=0.6` 弹性回弹至 1.0（250ms，OvershootInterpolator 2.5x）→ 恢复原位置 |

| 状态 | 视觉 | 动画 | 交互 |
|------|------|------|------|
| **空闲** | 静态圆形图标(48dp)，alpha 0.8 + 联系人标签(如有选中联系人) | 无 | 可点击、可拖拽 |
| **处理中** | 圆形隐藏，显示横向胶囊条(160dp×40dp)，内含 ProgressBar + 动态阶段文字，alpha 1.0 | 从球心展开弹出（280ms Decelerate） | 不可点击、不可拖拽（防重复） |
| **成功** | 恢复圆形 + 联系人标签，绿色对勾覆盖 | 弹性回弹 + 绿色闪光 | 不可点击，**3秒后自动回退到空闲状态** |
| **失败** | 恢复圆形 + 联系人标签，红色叉号覆盖 | 弹性回弹 + 红色闪烁 + 微弱振动 | 不可点击，**3秒后自动回退到空闲状态** |

#### 5.2.2 状态同步机制（v2.5 新增）

解决"返回App后球立刻变空闲但任务还在处理"的核心问题：

```
Dart 端                              Java 端
    │                                    │
    ├── _isProcessing = true ──────────→ dartIsProcessing = true
    │   (开始处理)                          (记录状态)
    │                                    │
    ├── updateBallStatus('processing') ──→ setBallState(STATE_PROCESSING)
    │                                    │
    ├── updateBallStatus('analyzing') ───→ 文字变"识别对话中..."
    │                                    │
    ├── updateBallStatus('generating') ──→ 文字变"生成建议中..."
    │                                    │
    ├── _isProcessing = false ─────────→ dartIsProcessing = false
    │   (处理完成/异常)                      (清除状态)
    │                                    │
    │                              onResume() 时：
    │                              if (dartIsProcessing)
    │                                → 自动恢复 STATE_PROCESSING 视觉
```

**关键设计点：**
- `dartIsProcessing` 是 Java 端布尔标志，不受 Activity 生命周期影响
- `onResume` 时检查此标志，若为 `true` 则恢复球的 processing 视觉（横条+旋转）
- App 被杀重启时，`floatingView == null` → 自动重建悬浮球 + 恢复联系人标签

#### 5.2.3 技术实现

Java 侧 `ACTION_UP` 检测到点击且非拖拽后：
1. 立即 `setImageResource(R.drawable.ball_processing)` + `setEnabled(false)` — **即时反馈**
2. 异步调用 `triggerScreenshot()` → Dart 流水线
3. 完成后 MethodChannel 回调 → 恢复图标 + 绿色/红色动画
4. `setEnabled(true)` + 恢复原图标

### 5.3 全局浮窗建议卡片

> **核心原则：建议卡片必须直接浮在聊天 App 上方，用户不需要切回 Chat-Helper。**

#### 5.3.1 技术方案

Java `WindowManager.addView()` 添加一个原生 View 浮窗：
- `TYPE_APPLICATION_OVERLAY` — 悬浮在所有 App 之上
- `FLAG_NOT_FOCUSABLE` — 不抢输入焦点
- `FLAG_NOT_TOUCH_MODAL` — 点击外部继续传递事件

#### 5.3.2 布局设计

```
┌──────────────────────────────────────────┐
│ 💡 对话思路              [📋] [⚡8s] [✕] │ ← 标题栏 + 存入草稿(v2.5) + 倒计时
├──────────────────────────────────────────┤
│  顺着对方的话题接，表现出你也想去，        │ ← Part1: 方向（蓝色底）
│  但加点俏皮感，别太急切                    │
├──────────────────────────────────────────┤
│  热情  好呀！上次去还是秋天呢              │ ← Part2: 具体话术
│        花开了肯定好看 🌸              │    [复制]
│        理由：积极回应 + 回忆共鸣            │
├──────────────────────────────────────────┤
│  幽默  这是在约我吗哈哈 去哪去哪            │
│        理由：轻松调侃，制造对话节奏          │
├──────────────────────────────────────────┤
│  稳妥  行，几点？我收拾一下                │
│        理由：简洁直接，不拖不带压力          │
├──────────────────────────────────────────┤
│          [换一批]               [关闭]    │
└──────────────────────────────────────────┘
```

| 区域 | 内容 | 说明 |
|------|------|------|
| **标题栏** | 💡 对话思路 + **📋存入草稿**(v2.5) + 倒计时 + ✕ | 新增书签图标按钮，点击后通知Dart端将当前建议保存到草稿箱 |
| **Part1 方向** | 模糊对话方向（1句话） | 蓝色浅底，15sp |
| **Part2 话术** | 3条具体话术（风格+内容+理由） | 每条可独立复制 |

#### 5.3.3 生命周期

| 事件 | 行为 |
|------|------|
| **出现** | AI-2 返回结果后，从底部滑入，300ms |
| **倒计时** | 默认 8 秒倒计时（可在设置中调整） |
| **点击复制** | 呼出 Toast "已复制"，倒计时重置+3秒延期 |
| **倒计时到0** | 浮窗淡出消失 |
| **点击 ✕** | 立即关闭 |
| **点击外部区域** | 无反应（避免误触） |
| **点击「换一批」** | 悬浮球先进入"处理中"横条状态 → 浮窗内 loading → 新建议替换 |
| **点击「关闭」** | 立即关闭 |

#### 5.3.4 设置项

| 配置项 | 选项 | 默认值 |
|--------|------|--------|
| **建议卡显示时间** | 5秒 / 8秒 / 15秒 / 手动关闭 | **8秒** |

### 5.4 豆包单次多任务调用（合并 AI-1 + AI-2）

> **V2 核心设计**：用**一次** HTTP 调用让豆包**同时**完成两件事——对话提取 + 回复建议。  
> 不再拆分 AI-1 / AI-2，省去串行等待，用户感知延时从 5-14s 降至 **3-6s**。

#### 5.4.1 调用流程

```
截图PNG → Base64 编码
       │
       ▼
加载当前联系人：
   ├ 最近 6 条记忆（增量去重基准，L3）
   ├ 全量历史记忆（建议生成参考，头尾保留策略：前20条+最近200条，硬上限300条，L2）
   ├ 联系人档案（语气/长度/创意度，L1）
   └ 当前时间戳（L5）
       │
       ▼
按模型名选择端点（自动判断）：
   ├ Seed 系列       → POST /responses
   └ 1.5 Vision Pro  → POST /chat/completions
       │
       ▼
单次 API 调用（Base URL：https://ark.cn-beijing.volces.com/api/v3）
       │
       ▼
解析返回 JSON：
   ├ new_messages[]       → 落 chat_memories（幂等校验）
   ├ scene + scene_description
   ├ contact_insight      → 落 contact_insight 字段
   ├ direction            → 浮窗 Part1
   └ suggestions[3]       → 落 suggestions 表 + 浮窗 Part2
       │
       ▼
浮窗卡片弹出（WindowManager，聊天App上方）
```

#### 5.4.2 上下文注入分层（详见 §11）

| 层 | 内容 | 用途 | 来源 |
|----|------|------|------|
| **L1** | 联系人档案 | 个性化参数 | contacts 表 |
| **L2** | 全量历史（头尾保留） | 联系人画像 + 建议生成参考 | chat_memories 全表，**硬上限 300 条：前 20 条 + 最近 200 条** |
| **L3** | 最近 6 条 | **增量去重判断基准** | chat_memories ORDER BY created_at DESC LIMIT 6 |
| **L4** | 截图 base64 | 视觉识别 | 内存 |
| **L5** | 当前时间 | 时间上下文 | DateTime.now() |
| **L6** | 任务指令 + JSON Schema | 引导输出 | Prompt 内置 |

#### 5.4.3 输出 JSON Schema（严格约束）

```json
{
  "new_messages": [
    {
      "speaker": "me|partner",
      "content": "消息文本",
      "time": "ISO8601 或相对时间字符串"
    }
  ],
  "scene": "A|B|C",
  "scene_description": "一句话描述当前场景",
  "contact_insight": "基于全量历史的联系人画像摘要（1-2 句话）",
  "direction": "对话方向（1 句话）",
  "suggestions": [
    {
      "style": "风格名（热情/幽默/稳妥/直白/温柔/补充/追问/收转...）",
      "content": "具体回复话术（me 发给 partner 的话）",
      "reason": "简短理由"
    },
    {
      "style": "...",
      "content": "...",
      "reason": "..."
    },
    {
      "style": "...",
      "content": "...",
      "reason": "..."
    }
  ]
}
```

#### 5.4.4 System Prompt（V2 多任务合并版）

```
你是一个高情商的聊天助手 + 聊天记录解析器的合体。图片是一个手机【两人私聊】窗口的截图。

【你的双重任务 — 必须在一次输出中同时完成】

▌任务 A：对话增量提取
  从截图中识别所有对话消息，与"已知历史"比对后，仅输出新增消息。

▌任务 B：回复建议生成
  根据"全量历史" + 任务 A 提取的新增消息 + 当前时间，输出 3 条 me 的回复建议。

═══════════════════════════════════════════════════
  第一部分：角色判定（任务 A & B 共用）
═══════════════════════════════════════════════════

这是【两人私聊】窗口，只有两个说话人：
1. 屏幕右半区气泡 → me（手机持有者 / 截图者 / 需要建议的一方）
2. 屏幕左半区气泡 → partner（对话对象）
3. 辅助确认：右侧气泡通常带绿色/蓝色背景色 = me
4. 辅助确认：左侧气泡通常带白色/灰色背景色 = partner

═══════════════════════════════════════════════════
  第二部分：联系人档案（任务 B 个性化）
═══════════════════════════════════════════════════

联系人姓名：{contact_name}
关系分类：{relationship}
语气偏好：{tone}
长度偏好：{length}
创意度  ：{creativity}/10

═══════════════════════════════════════════════════
  第三部分：全量历史（含 [self]/[other] 标记）
═══════════════════════════════════════════════════

以下是按时间顺序排列的全部对话记录，用于任务 B 生成建议时理解人物背景：
{full_history_with_speaker_tags}

如果历史过长，采用头尾保留策略：
  - 开头：保留最早 N 条（默认 20 条）
  - 结尾：保留最近 M 条（默认 180 条）
  - 中间被裁剪部分用 "[...省略 N 条对话...]" 替代

═══════════════════════════════════════════════════
  第四部分：最近 6 条记忆（任务 A 增量去重基准）
═══════════════════════════════════════════════════

以下 6 条是已存在于记忆库的最新消息，任务 A 提取时【不要重复输出】：
{recent_6_memories_json_array}

示例格式：
[
  {"speaker": "partner", "content": "在吗", "time": "2026-06-04T10:00:00"},
  {"speaker": "me", "content": "在呀", "time": "2026-06-04T10:01:00"},
  ...
]

═══════════════════════════════════════════════════
  第五部分：当前时间
═══════════════════════════════════════════════════

现在（截图时刻）：{current_time}
最后一条消息时间：{last_msg_time}
距最后一条消息：{time_elapsed}

═══════════════════════════════════════════════════
  第六部分：场景判断规则（任务 B 核心）
═══════════════════════════════════════════════════

请根据"距最后一条消息的时间间隔"和"最后一条消息的说话人"自动判断：

┌─────────────┬──────────────────────────┬──────────────────────────────┐
│ 场景         │ 判断条件                  │ me 的意图 & AI 应该做什么     │
├─────────────┼──────────────────────────┼──────────────────────────────┤
│ A. 待回复    │ 最后一条是 partner 发的，   │ me 需要回复 partner 的最新    │
│             │ 且时间间隔较短              │ 消息。建议应该回应内容、       │
│             │（几分钟~几小时）            │ 延续话题、表达态度             │
├─────────────┼──────────────────────────┼──────────────────────────────┤
│ B. 主动发起  │ 距最后一条消息已超过数小时   │ me 是主动想找对方聊天。        │
│             │ 甚至隔天                   │ 建议应该自然开场、提起话题、    │
│             │                          │ 轻松破冰。不要假装在"回复"     │
│             │                          │ 某条具体消息                  │
├─────────────┼──────────────────────────┼──────────────────────────────┤
│ C. 延续/追加 │ 最后一条是 me 发的          │ me 发了一条对方还没回，但 me   │
│             │（对方尚未回复），或 me      │ 想继续说。建议应该补充说明、   │
│             │ 想追加点什么                │ 转换话题、追问、或轻松收尾    │
└─────────────┴──────────────────────────┴──────────────────────────────┘

═══════════════════════════════════════════════════
  第七部分：方向铁律（任务 B 绝对不能违反）
═══════════════════════════════════════════════════

你生成的每一条 suggestion，都是 me 即将发给 partner 的话。
me 是发消息的人，不是等消息的人。

方向自检清单：
- "好哒，那我等你哦" ← 被动等待，是 partner 该说的话，【不要输出】
- "哈哈哈是啊" ← 单纯附和，没有推动对话，【不要输出】
- "好的呀""收到""没问题" ← 确认式语气，【不要输出】

正确示例（me 发给 partner）：
- "好呀！上次去还是秋天呢，花开了肯定好看" → 主动回应 + 回忆共鸣
- "这是在约我吗哈哈，去哪去哪" → 主动调侃
- "行，几点？我收拾一下" → 主动推进计划

═══════════════════════════════════════════════════
  第八部分：说人话铁律（任务 B — 去AI味全面优化 v2.2）
═══════════════════════════════════════════════════

【绝对禁止 ❌ — 以下7类AI典型病症，一条都不要有】

1. **公文腔/客服腔**：禁止"当然可以""很高兴为你服务""感谢您的耐心等待"
2. **心理咨询腔**：禁止"我理解你的感受""这确实是一个值得思考的问题""你的感受是合理的"
3. **过度礼貌**：禁止连续使用"请""您""麻烦了""不好意思"，真人聊天不会这样
4. **空洞表态**：禁止"我会一直支持你""无论发生什么我都在你身边"——没有具体内容
5. **完美句式**：禁止排比句、对仗句、"一方面...另一方面..."——这不是写作文
6. **情绪过载**：禁止连续使用多个感叹号+emoji堆叠，如"太棒了！！！💪✨🎉"
7. **教科书式总结**：禁止"综上所述""总而言之""总的来说"——微信里没人这样说话

【必须做到 ✅ — 真人聊天的7个特征】

1. **口语化省略**：主语经常省略，"几点？""走啊""行吧"都是完整句子
2. **有态度/有偏好**：可以表达个人喜好，"我不爱吃香菜""这个剧一般"
3. **具体不抽象**：说具体的事而不是泛泛而谈。"加班到10点"比"工作很忙"更像人
4. **长度自然变化**：3条建议不应该字数差不多。有的短（"哈哈 真的？"），有的长一点
5. **适当留白**：不需要每条回复都面面俱到。有时候一句反问就够了
6. **可以不正经**：允许调侃、自嘲、夸张。"你在凡尔赛吗""救命 这也太好看了"
7. **允许极简回应**：在快速模式下，"嗯嗯""好的呀""真的？" 都是合格回复

【AI vs 真人 对比示例表】

| 场景 | AI味回复（❌） | 真人回复（✅） |
|------|--------------|--------------|
| 对方说"今天加班好累" | "辛苦了！工作确实很消耗精力，记得好好休息哦 💪" | "又加班啊 几点走的" |
| 对方分享美食 | "看起来非常美味！感谢你的分享，让人食欲大开 🤤" | "看着不错 哪家的" |
| 对方说心情不好 | "我理解你现在的心情不好，如果需要倾诉我随时都在" | "怎么了 发生了啥" |
| 对方问周末安排 | "周末是个放松的好时机，你可以考虑去户外走走或者在家休息" | "还没想呢 你有安排没" |
| 开启新话题 | "最近有什么有趣的事情想和我分享吗？" | "对了 你看那个xxx了吗" |

> **核心原则**：每条建议生成后，先问自己——"这句话会出现在我的微信聊天记录里吗？" 如果不会，重写。

═══════════════════════════════════════════════════
  第九部分：输出格式（严格 JSON，不允许任何额外文字）
═══════════════════════════════════════════════════

{
  "new_messages": [
    {"speaker": "me|partner", "content": "消息内容", "time": "ISO8601或相对时间"}
  ],
  "scene": "A|B|C",
  "scene_description": "一句话描述当前场景",
  "contact_insight": "基于全量历史的联系人画像摘要（1-2 句话）",
  "direction": "用一句话说清这轮对话怎么回最合适",
  "suggestions": [
    {"style": "风格名", "content": "具体回复话术", "reason": "简短理由"},
    {"style": "风格名", "content": "具体回复话术", "reason": "简短理由"},
    {"style": "风格名", "content": "具体回复话术", "reason": "简短理由"}
  ]
}

⚠️ 边界规则：
- 如果 new_messages 为空（截图无新增），仍必须输出 scene / direction / suggestions
  （基于全量历史生成"主动破冰"或"继续话题"的建议）
- 如果全量历史也为空（首次对话），scene 强制为 B，direction 提示"开启新话题"
- contact_insight 即使历史为空也要输出"暂无足够信息"
- 如果截图无文字内容，new_messages 为空，scene 强制为 B
```

#### 5.4.5 调用参数

| 参数 | 值 |
|------|-----|
| `model` | `doubao-seed-2-0-mini-260428`（默认，可在设置切换） |
| `temperature` | 0.4（多任务场景比单一任务略高以保多样性） |
| `max_tokens` | 4096 |
| `stream` | false |
| `Base URL` | `https://ark.cn-beijing.volces.com/api/v3` |

#### 5.4.6 端点自动选择

| 模型 | 端点 |
|------|------|
| `doubao-seed-1-6-flash-250828` | `/responses` |
| `doubao-seed-1-6-vision-250815` | `/responses` |
| `doubao-seed-2-0-mini-260428` | `/responses` |
| `doubao-1-5-vision-pro-32k-250115` | `/chat/completions` |
| 自定义模型 | 名称含 `seed` → `/responses`；含 `1.5`/`vision-pro`/`vision-lite` → `/chat/completions`；兜底 `/responses` |

> 用户无需关心 endpoint 切换，由客户端按模型名自动判断。详见 §10。

#### 5.4.7 落库策略

| 输出字段 | 落库表 | 备注 |
|---------|--------|------|
| `new_messages[]` | `chat_memories` | 客户端做幂等校验（同 speaker+content 不重复入库） |
| `scene` + `scene_description` | `suggestions.scene` | 关联到本次调用 |
| `contact_insight` | `contacts.notes` 字段 / `contact_insights` 单独表 | 详见 §8 |
| `direction` | `suggestions.direction` | |
| `suggestions[3]` | `suggestions` 表（3 条记录） | |

详见 §12。

#### 5.4.8 异常处理

| 情况 | 处理 |
|------|------|
| 豆包超时/失败 | 悬浮球红色闪烁 + 振动，Toast "AI 提取失败，请重试" |
| 返回非 JSON | 客户端从 content 提取 JSON（支持 markdown ```json 包裹） |
| `new_messages` 解析失败 | 当作"无新增"处理，不阻塞建议生成 |
| `suggestions` 解析失败 | 浮窗显示"建议生成失败"占位 |
| API Key 未配置 | 浮窗提示"请先在设置页配置豆包 API Key" |

---

### 5.5 联系人档案管理

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 姓名 | String | 是 | |
| 关系分类 | Enum | 否 | 朋友/同事/家人/暧昧/自定义 |
| 语气偏好 | Enum | 否 | 默认"自然中性" |
| 长度偏好 | Enum | 否 | 默认"两三句" |
| 创意度偏好 | Float | 否 | 默认0.5 |

- 首页联系人Tab → 列表/搜索/快速切换
- 选中联系人高亮

---

### 5.6 话术草稿编辑器

- 从浮窗卡片点击进入
- 预填AI建议文本
- 自由修改 → 一键复制/保存

---

### 5.7 历史对话记录

- 按联系人筛选，时间线视图
- **切换联系人时自动刷新数据**（didUpdateWidget 生命周期）
- 时间倒序 + 关键词搜索
- 单条/批量删除
- 联系人数据完全隔离：记忆（chat_memories）、截图记录（screenshots）、建议（suggestions）均按 `contact_id` 独立存储和查询
- 不存在不可见的 `_default` 占位联系人——未选联系人时拦截截图，不产生孤立数据

---

### 5.8 设置与参数配置

#### 5.8.1 豆包 Seed Vision 配置

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| **豆包 API Key** | console.volcengine.com 获取 | - |
| **AI 模型** | 下拉选择 / 自定义 | `doubao-seed-2-0-mini-260428` |
| Base URL（固定） | 火山引擎方舟平台 | `https://ark.cn-beijing.volces.com/api/v3` |
| 端点 | **按模型名自动判断**，无需手动选择 | - |
| temperature | 全局默认 | 0.4 |
| max_tokens | 全局默认 | 4096 |

**模型下拉选项：**

| 显示名称 | 模型 ID | 端点 |
|---------|--------|------|
| Seed 2.0 Mini（推荐/默认） | `doubao-seed-2-0-mini-260428` | `/responses` |
| Seed 2.0 Pro | `doubao-seed-2-0-pro-260215` | `/responses` |
| 自定义 | 用户输入 | `/responses`（默认） |

> 所有 Seed 2.x 系列模型均使用 `/responses` 端点。

#### 5.8.2 联系人档案默认值

| 配置项 | 说明 | 可选值 | 默认值 |
|--------|------|--------|--------|
| **默认语气** | 适用于该联系人 | 轻松随意 / 正式得体 / 幽默风趣 / 温柔体贴 / 自然中性 | 自然中性 |
| **默认长度** | 适用于该联系人 | 一句话 / 两三句 / 详细展开 | 两三句 |
| **默认创意度** | 适用于该联系人 | 0.0-1.0 | 0.5 |

#### 5.8.3 显示设置

| 配置项 | 说明 | 选项 | 默认值 |
|--------|------|------|--------|
| **建议卡显示时间** | 浮窗自动消失时间 | 5秒 / 8秒 / 15秒 / 手动关闭 | **8秒** |
| **快速回复模式** | 开启后 AI 生成更简短、更直接的回复建议 | 开启 / 关闭 | **关闭** |

> **快速回复模式说明**：开启后，AI 提示词中会注入以下指令：
> - 回复要简短有力，不要长篇大论
> - 直接给出最实用的回复，不要过多分析或铺垫
> - 偏好使用短句、口语化表达，像真人秒回一样自然
> - 每条建议控制在 **5-15 字**以内（特殊情况不超过 20 字）
> - direction 控制在 **10-15 字**
> - 允许极简回应："嗯嗯""哈哈""真的？""好的呀"
>
> **快速回复示例**：
> | 场景 | AI味（❌） | 快速回复（✅） |
> |------|----------|--------------|
> | 对方约饭 | "当然可以！我很乐意和你一起用餐" | "走啊 几点" |
> | 分享好消息 | "太棒了！真为你感到高兴！" | "卧槽 牛啊" |
> | 问在干嘛 | "我正在处理一些工作事务" | "摸鱼呢 哈哈" |
> | 表达关心 | "希望你一切安好，注意保重身体" | "多穿点 别感冒了" |
>
> 适用场景：需要快速回复的即时通讯场景（如微信闲聊、群聊）。

#### 5.8.4 外观

- 浅色 / 深色模式

---

## 6. 交互流程总览

### 6.1 首次使用（一次性）

```
① 设置→无障碍→已安装的服务→开启 Chat-Helper
② 打开 App → 设置页 → 配置豆包 API Key（仅需一个）
③ 首页 → 创建联系人 → 选中
④ AppBar 点 ☰ → 开启悬浮球
⑤ 设置浮窗显示时间（默认8秒）
```

### 6.2 日常使用（支持三种触发场景）

> **V2 流水线**：所有场景共用同一条单次豆包调用——**一次截图、一次请求、一次响应**。差异仅在于豆包对场景的判断和返回的 suggestions 风格。

**场景A — 对方发了消息，我要回复**（最常见）：
```
在聊天App看到 partner 的新消息
       │
       ▼
点击悬浮球 → 【处理中】旋转动画 ⏳
       │
       ▼
截图 → 豆包单次多任务调用
       ├ 任务A: 提取(与最近6条比对) → 仅新增落库
       └ 任务B: 判定场景A → "partner刚发了消息，me需要回复"
       │
       ▼
┌──────────────────────────────────────┐
│ [A 待回复] 💡 顺着对方话题接... [8s] ✕│ ← 浮窗出现在聊天App上方
│  ────────────────────────────────── │
│  热情  好呀！花开了肯定好看         │
│  幽默  这是在约我吗哈哈             │
│  稳妥  行，几点？                   │
│          [换一批]           [关闭]  │
└──────────────────────────────────────┘
```

**场景B — 跨天/隔了很久，我想主动找对方聊**：
```
打开很久没联系的聊天窗口
       │
       ▼
点击悬浮球 → 【处理中】旋转动画 ⏳
       │
       ▼
截图 → 豆包单次多任务调用
       ├ 任务A: 提取 → 与历史比对 → 无新增（或仅少量）
       └ 任务B: 判定场景B → "距离上次聊天已很久，me主动破冰"
       │
       ▼
┌──────────────────────────────────────┐
│ [B 主动发起] 💬 自然重启对话... [8s] ✕│
│  ────────────────────────────────── │
│  随意  刚看到个事突然想到你          │
│  具体  对了上次说的展览这周末最后一天  │
│  幽默  失踪人口回归！在吗在吗        │
│          [换一批]           [关闭]  │
└──────────────────────────────────────┘
```

**场景C — 我刚发了一条对方没回，想继续说**：
```
我刚发了一条消息，对方还没回，但我想追加
       │
       ▼
点击悬浮球 → 【处理中】旋转动画 ⏳
       │
       ▼
截图 → 豆包单次多任务调用
       ├ 任务A: 提取 → 新增 me 的最新消息
       └ 任务B: 判定场景C → "me刚发了一条，对方还没回，me想继续说"
       │
       ▼
┌──────────────────────────────────────┐
│ [C 追加延续] 💬 补充或转换话题...[8s]✕│
│  ────────────────────────────────── │
│  补充  哦对了还有一件事忘了说         │
│  追问  你觉得呢？我还是有点纠结       │
│  收转  不说这个了，问你个事           │
│          [换一批]           [关闭]  │
└──────────────────────────────────────┘
```

> **通用后续操作**：点"复制" → Toast "已复制" → 贴回微信 → 浮窗延期3秒后消失 / 等待倒计时自动消失 → 悬浮球绿色闪光 ✓

### 6.3 端到端耗时（V2 优化后）

| 步骤 | 耗时 |
|------|------|
| 截图 | <0.5s |
| **豆包单次多任务调用** | **2.5-5.5s** |
| 客户端解析 + 落库 | <0.2s |
| 浮窗动画 | 0.3s |
| **总计** | **3-6s**（vs v1.5 的 5-14s，**缩短 50%**） |

---

## 7. UI 设计方向

- **悬浮球**：圆形48dp，半透明，有状态动画
- **浮窗卡片**：底部弹出，圆角24dp，半透明黑色蒙层，8秒后自动消失
- **深色模式**支持

---

## 8. 数据模型

```sql
-- ============================================================
-- 联系人表
-- ============================================================
CREATE TABLE contacts (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    relationship TEXT,                     -- 朋友/同事/家人/暧昧/自定义
    gender TEXT,
    notes TEXT,                            -- 用户备注 / V2 追加 contact_insight
    tone_preference TEXT DEFAULT 'neutral', -- 轻松随意/正式得体/幽默风趣/温柔体贴/自然中性
    length_preference TEXT DEFAULT 'medium', -- 一句话/两三句/详细展开
    creativity_preference REAL DEFAULT 0.5, -- 0.0-1.0
    latest_insight TEXT,                   -- V2 新增：最近一次豆包返回的 contact_insight
    latest_insight_at INTEGER,             -- V2 新增：最近一次更新时间
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    last_active_at INTEGER
);
CREATE INDEX idx_contacts_active ON contacts(last_active_at DESC);

-- ============================================================
-- 对话记忆表（增量去重落库）
-- ============================================================
CREATE TABLE chat_memories (
    id TEXT PRIMARY KEY,
    contact_id TEXT NOT NULL,
    screenshot_id TEXT,                    -- 关联到本次截图（可空，导入的历史无截图）
    speaker TEXT NOT NULL CHECK(speaker IN ('me', 'partner')),
    content TEXT NOT NULL,
    msg_time TEXT,                         -- V2 新增：消息时间（AI 提取时输出，可空）
    created_at INTEGER NOT NULL,           -- 入库时间（用于最近 6 条查询）
    UNIQUE(contact_id, speaker, content),  -- 幂等防重
    FOREIGN KEY (contact_id) REFERENCES contacts(id) ON DELETE CASCADE
);
CREATE INDEX idx_memories_contact ON chat_memories(contact_id, created_at);
CREATE INDEX idx_memories_recent ON chat_memories(contact_id, created_at DESC);

-- ============================================================
-- 截图记录表
-- ============================================================
CREATE TABLE screenshots (
    id TEXT PRIMARY KEY,
    contact_id TEXT,
    file_path TEXT NOT NULL,
    ai_result_json TEXT,                   -- V2 改：一次返回的完整 JSON
    scene TEXT,                            -- V2 新增：A/B/C
    scene_description TEXT,                -- V2 新增：场景描述
    contact_insight TEXT,                  -- V2 新增：联系人画像
    direction TEXT,                        -- V2 新增：对话方向
    ai_status TEXT DEFAULT 'pending',      -- pending/success/failed
    ai_error TEXT,                         -- 失败原因
    duration_ms INTEGER,                   -- V2 新增：单次调用耗时（用于监控）
    created_at INTEGER NOT NULL,
    FOREIGN KEY (contact_id) REFERENCES contacts(id) ON DELETE SET NULL
);
CREATE INDEX idx_screenshots_contact ON screenshots(contact_id, created_at DESC);

-- ============================================================
-- 建议表（每次豆包返回的 3 条话术 + 场景方向）
-- ============================================================
CREATE TABLE suggestions (
    id TEXT PRIMARY KEY,
    contact_id TEXT NOT NULL,
    screenshot_id TEXT,                    -- 关联到本次截图调用
    scene TEXT,                            -- V2 新增：A/B/C
    scene_description TEXT,                -- V2 新增：场景描述
    direction TEXT,                        -- V2 新增：对话方向
    style TEXT,                            -- 热情/幽默/稳妥/直白/温柔/补充/追问/收转...
    content TEXT NOT NULL,
    reason TEXT,
    is_favorited INTEGER DEFAULT 0,
    is_copied INTEGER DEFAULT 0,
    created_at INTEGER NOT NULL,
    FOREIGN KEY (contact_id) REFERENCES contacts(id) ON DELETE CASCADE
);
CREATE INDEX idx_suggestions_contact ON suggestions(contact_id, created_at DESC);
CREATE INDEX idx_suggestions_screenshot ON suggestions(screenshot_id);
```

### 8.1 V2 关键字段说明

| 字段 | 表 | 说明 |
|------|------|------|
| `scene` | `screenshots` / `suggestions` | A=待回复 / B=主动发起 / C=延续追加 |
| `scene_description` | `screenshots` / `suggestions` | 一句话描述当前场景 |
| `contact_insight` | `screenshots` / `contacts.latest_insight` | 豆包基于全量历史的画像摘要 |
| `direction` | `screenshots` / `suggestions` | 对话方向（浮窗 Part1） |
| `msg_time` | `chat_memories` | 消息时间（AI 提取时输出，可能为空，fallback 用 `created_at`） |
| `duration_ms` | `screenshots` | 单次 API 调用耗时（性能监控） |
| `ai_error` | `screenshots` | 失败原因（用于排查） |
| `UNIQUE(contact_id, speaker, content)` | `chat_memories` | 客户端幂等校验：避免同一条消息被多次入库 |

### 8.2 索引策略

| 索引 | 用途 |
|------|------|
| `idx_memories_recent` | 高频查询：最近 6 条记忆（增量去重基准） |
| `idx_screenshots_contact` | 高频查询：某联系人最近截图记录 |
| `idx_suggestions_screenshot` | 关联查询：本次调用的所有建议 |

---

## 9. 技术可行性评估

| 层级 | 技术 |
|------|------|
| **截图** | AccessibilityService.takeScreenshot() |
| **AI 引擎（V2 唯一）** | 豆包 Seed Vision / 1.5 Vision Pro（多任务单次调用） |
| **HTTP** | Dart `http` package + `dart:convert` |
| **存储** | SQLite + SharedPreferences + FlutterSecureStorage |
| **悬浮球** | Android WindowManager (Java) |
| **浮窗卡片** | Android WindowManager 原生 View (Java) |
| **UI** | Flutter + Provider |

### 权限

| 权限 | 用途 |
|------|------|
| `SYSTEM_ALERT_WINDOW` | 悬浮球 + 浮窗卡片 |
| `BIND_ACCESSIBILITY_SERVICE` | 无感截图 |
| `FOREGROUND_SERVICE` | 保活 |
| `INTERNET` | 豆包 |

### 第三方依赖

| 依赖 | 用途 |
|------|------|
| `http` | 调用豆包 REST API |
| `sqflite` | 本地 SQLite 存储 |
| `path_provider` | 获取文件目录（截图存储） |
| `flutter_secure_storage` | 保存豆包 API Key（加密） |
| `shared_preferences` | 全局配置项（浮窗时间、模型选择） |
| `provider` | 状态管理 |

---

## 10. API 集成规范（V2 豆包单引擎）

### 10.1 端点与模型对照

V2 仅有一个 AI 引擎（豆包），但**不同模型对应不同端点格式**（与火山引擎官方 Python SDK 完全一致）：

| 模型 | 端点 | 官方 SDK 调用 | Base URL |
|------|------|--------------|----------|
| `doubao-seed-1-6-flash-250828` | `/responses` | `client.responses.create()` | `https://ark.cn-beijing.volces.com/api/v3` |
| `doubao-seed-1-6-vision-250815`（推荐） | `/responses` | `client.responses.create()` | `https://ark.cn-beijing.volces.com/api/v3` |
| `doubao-seed-2-0-mini-260428` | `/responses` | `client.responses.create()` | `https://ark.cn-beijing.volces.com/api/v3` |
| `doubao-1-5-vision-pro-32k-250115` | `/chat/completions` | `client.chat.completions.create()` | `https://ark.cn-beijing.volces.com/api/v3` |

**端点自动判断规则**（用户无需手动切换）：

1. 预设模型查表（参见上表）
2. 自定义模型：包含 `seed` → `/responses`；包含 `1.5` / `vision-pro` / `vision-lite` → `/chat/completions`
3. 兜底：默认 `/responses`

### 10.2 /responses 端点 curl 示例（Seed 系列，V2 单次多任务）

> **核心请求结构**：`input` 顶层（不是 `messages`），`role: "user"` 包一个 `content` 列表，列表中可同时含 `input_image` + `input_text`。`system prompt` 合并到 `input_text` 内（不单独放 `system` 字段）。

```bash
curl https://ark.cn-beijing.volces.com/api/v3/responses \
  -H "Authorization: Bearer $ARK_API_KEY" \
  -H 'Content-Type: application/json' \
  -d '{
    "model": "doubao-seed-1-6-vision-250815",
    "input": [
        {
            "role": "user",
            "content": [
                {
                    "type": "input_image",
                    "image_url": "data:image/png;base64,<BASE64_OF_SCREENSHOT>"
                },
                {
                    "type": "input_text",
                    "text": "你是一个高情商的聊天助手 + 聊天记录解析器的合体。\n\n【你的双重任务】\nA. 对话增量提取：识别截图中新增消息（与已知最近 6 条比对）\nB. 回复建议生成：根据全量历史 + 当前时间，输出 3 条 me 的回复 + 方向 + 场景\n\n【联系人档案】\n姓名：小美  关系：朋友  语气：轻松随意  长度：两三句  创意度：5/10\n\n【全量历史 — 头尾保留 20+180】\n[2026-06-01 09:00] [self] 早上好呀\n[2026-06-01 09:01] [other] 早～\n...\n[2026-06-05 14:25] [other] 周末一起去那个花展呀？\n[2026-06-05 14:26] [self] 好啊，一直想去\n[...省略 50 条对话...]\n\n【最近 6 条记忆 — 任务 A 去重基准】\n[{\"speaker\":\"other\",\"content\":\"周末一起去那个花展呀？\",\"time\":\"2026-06-05T14:25\"},\n {\"speaker\":\"self\",\"content\":\"好啊，一直想去\",\"time\":\"2026-06-05T14:26\"}]\n\n【当前时间】\n现在：2026-06-05T14:30\n最后一条消息：2026-06-05T14:26\n距最后一条消息：4 分钟\n\n【输出严格 JSON】\n{\n  \"new_messages\": [...],\n  \"scene\": \"A|B|C\",\n  \"scene_description\": \"...\",\n  \"contact_insight\": \"...\",\n  \"direction\": \"...\",\n  \"suggestions\": [{\"style\":\"...\",\"content\":\"...\",\"reason\":\"...\"} ×3]\n}"
                }
            ]
        }
    ],
    "temperature": 0.4,
    "max_tokens": 4096
}'
```

**响应示例**（节选 `output` 节点）：

```json
{
  "id": "resp-xxxxxxxx",
  "model": "doubao-seed-1-6-vision-250815",
  "output": [
    {
      "type": "message",
      "role": "assistant",
      "content": [
        {
          "type": "output_text",
          "text": "{\n  \"new_messages\": [],\n  \"scene\": \"A\",\n  \"scene_description\": \"对方邀请周末一起去看花展，me 需要回复\",\n  \"contact_insight\": \"小美是 me 的好朋友，近期联系频繁，互动轻松愉快\",\n  \"direction\": \"顺着对方的话题接，表现出兴趣但加点俏皮感别太急切\",\n  \"suggestions\": [\n    {\"style\":\"热情\",\"content\":\"好呀！上次去还是秋天呢 花开了肯定好看\",\"reason\":\"积极回应 + 回忆共鸣\"},\n    {\"style\":\"幽默\",\"content\":\"这是在约我吗哈哈 去哪去哪\",\"reason\":\"轻松调侃，制造对话节奏\"},\n    {\"style\":\"稳妥\",\"content\":\"行，几点？我收拾一下\",\"reason\":\"简洁直接，不拖不带压力\"}\n  ]\n}"
        }
      ]
    }
  ],
  "usage": {
    "input_tokens": 4821,
    "output_tokens": 287,
    "total_tokens": 5108
  }
}
```

> **响应路径**：`response.output[0].content[0].text`（不是 OpenAI 习惯的 `choices[0].message.content`）。  
> 客户端解析时需兼容 `/responses` 端点特有的 `output` 嵌套结构。

### 10.3 /chat/completions 端点 curl 示例（1.5 Vision Pro）

> 1.5 Vision Pro 使用 OpenAI 兼容格式：顶层 `messages[]`，content 元素是 `image_url` + `text`。

```bash
curl https://ark.cn-beijing.volces.com/api/v3/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $ARK_API_KEY" \
  -d '{
    "model": "doubao-1-5-vision-pro-32k-250115",
    "messages": [
        {
            "role": "user",
            "content": [
                {
                    "type": "image_url",
                    "image_url": {"url": "data:image/png;base64,<BASE64_OF_SCREENSHOT>"}
                },
                {
                    "type": "text",
                    "text": "你是一个高情商的聊天助手 + 聊天记录解析器的合体。\n\n...(完整 system_prompt + 上下文注入，见 §5.4.4)..."
                }
            ]
        }
    ],
    "temperature": 0.4,
    "max_tokens": 4096
}'
```

**响应示例**：

```json
{
  "id": "chatcmpl-xxxxxxxx",
  "object": "chat.completion",
  "created": 1749123456,
  "model": "doubao-1.5-vision-pro-32k-250115",
  "choices": [
    {
      "index": 0,
      "message": {
        "role": "assistant",
        "content": "{\"new_messages\":[],\"scene\":\"A\", ...}"
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 4821,
    "completion_tokens": 287,
    "total_tokens": 5108
  }
}
```

> **响应路径**：`response.choices[0].message.content`（OpenAI 习惯路径）。  
> 客户端解析时需按端点选择不同的解析路径——这正是 `endpointTypeFor()` 函数存在的意义。

### 10.4 两个端点对比速查

| 维度 | `/responses`（Seed 系列） | `/chat/completions`（1.5 Vision Pro） |
|------|---------------------------|---------------------------------------|
| **请求体顶层** | `input` | `messages` |
| **消息 role** | `user` / `assistant` / `system` | `user` / `assistant` / `system` |
| **图片类型** | `input_image` | `image_url` |
| **文本类型** | `input_text` | `text` |
| **响应顶层** | `output[0].content[0].text` | `choices[0].message.content` |
| **使用模型** | `doubao-seed-1-6-*`、`doubao-seed-2-0-*` | `doubao-1-5-vision-pro-32k-*`、`doubao-1-5-vision-lite-*` |
| **推荐** | ✅ V2 默认 | 备选 |

### 10.5 模型选择方式

采取与 v1.5 DeepSeek 选择器相同的方式：在设置页面提供下拉选择框，用户可选择预设模型或输入自定义模型ID。

**预设模型列表：**

| 显示名称 | 模型ID | 端点 |
|---------|--------|------|
| Seed 2.0 Mini（推荐/默认） | `doubao-seed-2-0-mini-260428` | `/responses` |
| Seed 2.0 Pro | `doubao-seed-2-0-pro-260215` | `/responses` |
| 自定义 | 用户输入 | `/responses`（默认） |

> **端点由系统按模型名自动选择**，所有 Seed 2.x 系列均使用 `/responses` 端点。

### 10.6 异常处理

| 情况 | 处理 |
|------|------|
| 豆包超时（>10s） | 悬浮球红色闪烁 + 振动，Toast "AI 响应超时，请重试" |
| 豆包 HTTP 4xx/5xx | 解析 error 字段，悬浮球红色，Toast 显示错误码 |
| 豆包返回非 JSON | 客户端正则提取 `\{[\s\S]*\}`，提取后再次解析 |
| `new_messages` 解析失败 | 当作"无新增"处理，不阻塞建议生成（浮窗仍展示） |
| `suggestions` 解析失败 | 浮窗显示"建议生成失败，请稍后重试"占位 |
| API Key 未配置 | 首次点击悬浮球时弹 Toast "请先在设置页配置豆包 API Key" |
| 网络断开 | 悬浮球红色，Toast "网络不可用，请检查连接" |
| 截图为空 | 浮窗提示"截图失败，请确认无障碍服务已开启" |

### 10.7 客户端实现要求

| 要求 | 说明 |
|------|------|
| **超时** | 60 秒（豆包多任务推理本身需 3-6s，预留充足 buffer） |
| **重试** | 最多 2 次（超时/网络错误间隔 2s 重试；HTTP 4xx/5xx 不重试） |
| **并发** | 单线程串行（点击悬浮球期间禁用再次点击） |
| **流式** | 不启用（豆包视觉模型流式收益小，增加解析复杂度） |
| **客户端解析鲁棒性** | 兼容 `output[0].content[0].text` 和 `choices[0].message.content` 两种路径 |
| **客户端降级** | 解析失败时保留原始 content 到 `screenshots.ai_result_json` 供排查 |

---

## 11. 上下文注入规范（V2 重点）

### 11.1 设计动机

V2 合并了 AI-1 和 AI-2 为**一次调用**，但**豆包需要同时拿到**两类上下文：

1. **增量去重所需的"最近 6 条"**（任务 A）
2. **建议生成所需的"全量历史"**（任务 B）

如果直接把全量历史当作去重基准，会因 token 过大而拖慢；  
如果只传最近 6 条做建议生成，又会因为缺乏人物背景而答得很肤浅。

**解决方案**：上下文按用途**分层注入**——L1 ~ L6 互不重叠。

### 11.2 分层结构

| 层 | 名称 | 来源 | 用途 | 典型 token |
|----|------|------|------|------------|
| **L1** | 联系人档案 | `contacts` 表 | 个性化 prompt（tone/length/creativity） | ~100 |
| **L2** | 全量历史（头尾保留） | `chat_memories` 全表 | 联系人画像 + 建议生成参考 | 100-5000 |
| **L3** | 最近 6 条 | `chat_memories` ORDER BY created_at DESC LIMIT 6 | **增量去重判断基准** | ~150 |
| **L4** | 截图 base64 | 内存 | 视觉识别 | 500-2000 |
| **L5** | 当前时间 | `DateTime.now()` | 时间上下文 | ~30 |
| **L6** | 任务指令 + JSON Schema + 快速回复指令 | Prompt 内置 + 设置开关 | 引导输出 | ~200-300 |

**总 token 估算**：~2000-8000（豆包 1.6 Vision 16k 上下文够用）

### 11.3 L2 全量历史的"头尾保留"策略

**问题**：联系人聊了几个月可能有 1000+ 条记忆，token 爆炸。

**方案**：
- 开头：保留最早 **N_head = 20** 条（建立初始印象）
- 结尾：保留最近 **N_tail = 180** 条（最近对话上下文）
- 中间：裁剪，用 `[...省略 N 条对话...]` 占位
- 总数：≤ 200 条可控范围

**实现位置**：`DatabaseHelper.getMemoriesForContext(contactId)`，单次查询，按 `created_at` 排序后切片。

### 11.4 L3 与 L2 的关系

L3 是 L2 的**子集**（必然包含在末尾 180 条里），但**作为单独 Section 注入 prompt**，任务 A 才能明确"以这 6 条为基准去重"。

```
┌──────────────────────────────────┐
│ L2: 全量历史（20 + 180 条）       │  ← 任务 B 用
│   ... 开头 20 条                  │
│   [...省略 N 条对话...]           │
│   ... 结尾 180 条                 │
│   ├─ 末尾 6 条 ←── 即 L3         │  ← 任务 A 用
└──────────────────────────────────┘
```

**不去重 L2 与 L3**——冗余注入，明确告诉豆包"这 6 条是去重基准"。

### 11.5 注入顺序

> 顺序影响模型 attention。推荐顺序如下：

```
System Prompt
  ├ 第 1 部分：角色判定（两人私聊规则）
  ├ 第 2 部分：联系人档案（L1）
  ├ 第 3 部分：全量历史（L2，头尾保留）
  ├ 第 4 部分：最近 6 条记忆（L3，单独 section）
  ├ 第 5 部分：当前时间（L5）
  ├ 第 6 部分：场景判断规则
  ├ 第 7 部分：方向铁律
  ├ 第 8 部分：说人话铁律
  └ 第 9 部分：输出格式（JSON Schema）
User Input
  ├ 截图 base64（L4）
  └ 任务指令（"请完成 A + B，输出严格 JSON"）
```

### 11.6 时间格式化辅助函数

`time_elapsed` 字段需在客户端格式化为人类可读文本：

| 差值 | 格式 | 示例 |
|------|------|------|
| `< 1分钟` | "刚刚" | `刚刚` |
| `< 1小时` | "N 分钟前" | `4 分钟前` |
| `1-24小时` | "N 小时前" | `4 小时前` |
| `1-7天` | "N 天前" | `3 天前` |
| `>= 7天` | `YYYY-MM-DD` | `2026-05-28` |

**实现位置**：`lib/utils/time_formatter.dart`

---

## 12. 增量去重与落库策略

### 12.1 增量去重设计

#### 12.1.1 为什么把去重完全交给豆包

| 方案 | 优 | 劣 |
|------|----|----|
| 客户端文本匹配 | 零成本、快 | 处理不了"嗯"+"嗯嗯"+" 嗯 "这种语义重复 |
| **豆包视觉模型比对** | 利用多模态理解，能识别表情/标点/排版差异 | 多消耗 ~50 token |
| 双保险（AI + 客户端） | 严谨 | 增加复杂度，价值不大 |

V2 采用**豆包单源去重** + **客户端 UNIQUE 约束兜底**。

#### 12.1.2 流程

```
1. 查询 chat_memories ORDER BY created_at DESC LIMIT 6 → 构造 L3 注入 prompt
2. POST /responses（or /chat/completions）
3. 解析返回 JSON，取 new_messages 数组
4. 客户端对每条 new_message 做 UNIQUE 校验（contact_id, speaker, content）
   ├ 通过 → INSERT
   └ 失败（重复） → SKIP，记日志
5. 不管 L2 提取结果如何，scene / direction / suggestions 都要落库和展示
```

### 12.2 落库策略

#### 12.2.1 各表职责

| 表 | 写时机 | 数据 |
|----|--------|------|
| `screenshots` | 每次调用前预创建（status=pending），完成后更新（status=success/failed） | 完整请求/响应 |
| `chat_memories` | 解析 `new_messages` 后逐条 INSERT（幂等） | 新增对话 |
| `suggestions` | 解析 `suggestions[]` 后批量 INSERT 3 条 | 场景+方向+话术 |
| `contacts.latest_insight` | 解析 `contact_insight` 后 UPDATE | 联系人画像 |

#### 12.2.2 落库时机

| 步骤 | 落库 |
|------|------|
| 调用前 | `INSERT INTO screenshots (id, contact_id, file_path, ai_status='pending', created_at)` |
| 调用成功 | `UPDATE screenshots SET ai_result_json=?, scene=?, scene_description=?, contact_insight=?, direction=?, ai_status='success', duration_ms=? WHERE id=?` |
| 调用失败 | `UPDATE screenshots SET ai_status='failed', ai_error=? WHERE id=?` |
| 解析 new_messages | `INSERT INTO chat_memories (...) ON CONFLICT(contact_id, speaker, content) DO NOTHING` × N |
| 解析 suggestions | **批量查重后 INSERT**：先查出已存在的 content 集合 → 一次过滤 → 仅插入新建议（N 条建议从 N 次 RTT 降为 1-2 次 DB 查询） |
| 解析 contact_insight | `UPDATE contacts SET latest_insight=?, latest_insight_at=? WHERE id=?` |

#### 12.2.3 失败容忍

| 失败点 | 处理 |
|--------|------|
| `new_messages` 解析失败 | 跳过落库，不阻塞 suggestions 落库 |
| `suggestions` 解析失败 | 跳过落库，不阻塞 new_messages 落库 |
| 某条 new_message UNIQUE 冲突 | 跳过该条，继续处理后续 |
| `UPDATE contacts` 失败 | 不影响本次主流程，下次调用会自然覆盖 |

### 12.3 幂等保证

**数据库层**：
```sql
CREATE UNIQUE INDEX idx_memories_dedup
  ON chat_memories(contact_id, speaker, content);
```

**应用层**：
```dart
// 在 insertChatMemory 中
await db.insert(
  'chat_memories',
  memory.toMap(),
  conflictAlgorithm: ConflictAlgorithm.ignore,
);
```

**效果**：用户连点 3 次悬浮球（或并发点击），最终记忆库只保留 1 条。

### 12.4 "无新增"时的行为

当 `new_messages = []`（截图相对记忆库无新增），仍要：

1. 落库 `screenshots`（status=success, scene=A/B/C, direction=...）
2. 落库 `suggestions` × 3（基于全量历史生成的建议）
3. 浮窗正常弹出
4. 不显示任何 "未检测到新消息" 提示

**理由**：用户点悬浮球的目的是**获得建议**，去重只是顺带。即使没新增对话，给他"如何主动破冰"或"如何延续话题"的建议也是有价值的。

### 12.5 "全量历史为空"时的行为

`chat_memories` 表中无任何记录（首次对话）：

- `scene` 强制为 `B`（主动发起）
- `direction` 提示"开启新话题"（如 "用轻松的话题自然开启对话"）
- `contact_insight` 输出"暂无足够信息"
- `new_messages` 从截图提取的**全部消息**都视为新增

## 13. 关键评审事项与建议（提交开发前必看）

> 以下内容由产品经理梳理，**请所有利益相关者（技术、设计、测试、PM）逐条 review 后再进入开发**。

### 13.1 待确认的需求

| # | 议题 | 建议 | 决策人 |
|---|------|------|--------|
| 1 | **单次调用 token 上限风险** | 豆包 1.6 Vision 上下文窗口 16k，估算 2-8k 占用；建议上线前用真实长历史（500 条）做压测 | 技术 |
| 2 | **`contact_insight` 是否每次都更新** | 当前设计"覆盖式"更新，会丢失历史洞察；建议改为"每 N 次调用更新一次"或保留最近 3 个版本 | PM |
| 3 | **联系人档案中的"创意度"和"长度"在设置页和联系人详情页的关系** | 是全局默认 + 联系人覆盖，还是仅联系人级？建议前者 | PM/设计 |
| 4 | **是否支持群聊截图** | 当前 PRD 仅支持两人私聊。群聊识别复杂度高（多角色判断、消息归属），建议放到 V3 | PM |
| 5 | **截图中含敏感信息（密码、验证码）怎么办** | 建议在截图前/后做 OCR 敏感词检测，命中则阻断调用并提示用户 | 技术/PM |
| 6 | **API Key 存储方式** | 当前建议 FlutterSecureStorage，需确认 Android 端是否需要 Keystore 绑定 | 技术 |
| 7 | **豆包调用失败重试是否要做"快速重试"按钮** | 浮窗上加一个"↻ 重试"按钮，失败时一键重发 | 设计 |

### 13.2 技术风险点

| # | 风险 | 影响 | 建议应对 |
|---|------|------|----------|
| 1 | **豆包多任务响应时间不稳定** | 用户感知延时波动 | 浮窗加 loading 动画 + 进度文案；超时阈值设 10s（预留 buffer） |
| 2 | **`/responses` 端点响应路径与 OpenAI 不同** | 解析代码易出错 | 单元测试覆盖两种端点 + 显式文档化路径差异 |
| 3 | **截图 base64 占用内存峰值** | 1-2MB PNG → ~1.3MB base64，可能 OOM | 截屏后立即压缩到 ≤1MB（保留对话可读性） |
| 4 | **豆包识别出错（漏识/误识）** | 用户看到错误的话术 | 浮窗"换一批"按钮 + 建议附"理由"字段，用户可判断 |
| 5 | **浮窗在某些系统 App（如支付、相机）上被屏蔽** | 关键时刻无法用 | 启动时检测 WindowManager 添加结果，失败时降级到全屏 Activity |
| 6 | **无障碍服务被系统关闭** | 截图失败 | 监听 AccessibilityManager 状态，关闭时通知栏提醒 |
| 7 | **跨进程数据传递**（Java → Dart）大 payload | 序列化/反序列化阻塞 UI | 用 File 路径传截图，不走 MethodChannel |

### 13.3 待讨论的优化点

| # | 主题 | 当前方案 | 备选 |
|---|------|----------|------|
| 1 | **浮窗关闭交互** | 倒计时自动关 | 加上"点击浮窗外区域立即关"选项 |
| 2 | **联系人画像持久化策略** | 每次都更新 `latest_insight` | 加"画像时间线"页面看历史变化 |
| 3 | **支持多语言** | 仅中文 | V3 增加英文 prompt 模板 |
| 4 | **支持自定义 prompt** | 否 | 进阶用户可编辑 system prompt 片段（高风险，谨慎） |
| 5 | **浮窗 UI 风格** | 浅色卡片 | 提供"对话气泡"风格、"便签纸"风格等多种 skin |

### 13.4 验收标准（上线 Gate）

| 维度 | 标准 |
|------|------|
| **延时** | P95 ≤ 6s（单次豆包调用） |
| **增量去重准确率** | ≥ 95%（同一条消息不重复入库） |
| **建议可用率** | ≥ 80%（3 条建议中至少 1 条用户点击复制） |
| **场景判断准确率** | ≥ 85%（人工 review 100 个样本） |
| **崩溃率** | < 0.5%（基于截图→返回全流程） |
| **截图失败率** | < 5%（已开无障碍前提下） |
| **API 调用成功率** | ≥ 98%（4xx/5xx 占比 < 2%） |

### 13.5 数据埋点需求

| 事件名 | 触发时机 | 关键字段 |
|--------|----------|----------|
| `floating_ball_click` | 用户点击悬浮球 | contact_id, current_app |
| `screenshot_taken` | 截图成功 | file_size, duration_ms |
| `ai_call_start` | 豆包调用开始 | model, endpoint |
| `ai_call_success` | 豆包调用成功 | duration_ms, input_tokens, output_tokens, scene |
| `ai_call_failed` | 豆包调用失败 | error_code, error_message |
| `ai_call_timeout` | 豆包调用超时 | duration_ms |
| `suggestion_shown` | 浮窗展示 | screenshot_id, suggestions_count |
| `suggestion_copied` | 用户点击复制 | suggestion_id, style |
| `suggestion_edit_opened` | 用户进入草稿编辑 | suggestion_id |
| `overlay_dismissed` | 浮窗关闭 | reason (timeout/manual/copy) |
| `settings_changed` | 设置项变更 | key, old_value, new_value |

> **埋点是后续迭代的依据**，P1 之前完成核心事件埋点即可。

### 13.6 文档待补充项

| # | 内容 | 优先级 |
|---|------|--------|
| 1 | 浮窗卡片 UI 高保真原型（Figma） | P0 |
| 2 | 浮窗卡片 XML 布局技术规范（已有 `suggestion_overlay.xml` 需更新） | P0 |
| 3 | 联系人详情页 UI 稿 | P1 |
| 4 | 设置页 UI 稿 | P0 |
| 5 | 历史对话时间线 UI 稿 | P1 |
| 6 | 草稿编辑器 UI 稿 | P2 |
| 7 | 错误码对照表（含豆包 error code 与客户端处理逻辑） | P0 |
| 8 | 国际化（i18n）字符串规划 | P2 |

---

> **文档结束。Chat-Helper v2.0 产品需求基线（单 AI 引擎多任务版）。**
>
> **v2.0 核心变更：**
> - **架构重构**：双AI（豆包+DeepSeek）→ **单AI（豆包一次性完成对话提取+回复建议）**
> - **延时优化**：端到端延时 5-14s → **3-6s**（缩短 50%）
> - **API Key 减半**：2 个 → **1 个**，降低用户配置成本
> - **设置页简化**：移除 AI-2 DeepSeek 区块
> - **上下文分层**：新增 L1~L6 分层注入，头尾保留策略解决长历史 token 爆炸
> - **增量去重完全交给豆包**：利用多模态语义理解，客户端 UNIQUE 约束兜底
> - **新数据模型**：`screenshots` 表新增 `scene` / `scene_description` / `contact_insight` / `direction` / `duration_ms` / `ai_error` 字段
> - **`chat_memories` 表升级**：新增 `msg_time` 字段，新增 `UNIQUE(contact_id, speaker, content)` 约束，新增 `idx_memories_recent` 索引
> - **`suggestions` 表升级**：新增 `scene` / `scene_description` 字段，新增 `idx_suggestions_screenshot` 索引
> - **`contacts` 表升级**：新增 `latest_insight` / `latest_insight_at` 字段
> - **统一边界规则**：明确规定 "无新增但有建议"、"全量历史为空" 等边界场景的处理
> - **新增 §11 上下文注入规范、§12 增量去重与落库策略** 章节

---

## 14. 未来路线图（v2.4+）

> 以下为基于 v2.3 现状的产品优化建议，按优先级排序。涵盖**流程、功能、体验、代码、性能**五个维度。

### 14.1 P0 — 必须尽快修复

| # | 改动 | 说明 | 影响 |
|---|------|------|------|
| **1** | ~~截图前置校验联系人~~ ✅ v2.2 | 未选联系人时拦截截图，不产生孤立数据到 `_default` | 防止无效截图浪费 + 数据归属混乱 |
| **2** | ~~浮窗标题中文化~~ ✅ v2.2 | 标题从英文改为中文，如"为您准备了 N 条回复建议" | 用户感知直接 |

### 14.2 P1 — 重要改进

| # | 改动 | 说明 | 工作量 |
|---|------|------|--------|
| **3** | ~~落库逻辑去重~~ ✅ v2.2 | 抽取 `_persistAiResult()` 私有方法统一处理增量消息→画像更新→建议落库 | 中 |
| **4** | ~~清理死代码~~ ✅ v2.2/v2.3 | 移除废弃的 `captureScreenshot()` / `_handleFloatingBallClick()` / Java端 `captureScreenshot(Result)` 入口 | 小 |
| **5** | ~~建议批量查重~~ ✅ v2.2 | N 条建议从逐条 `findSuggestionByContent`（N次DB查询）改为先查出已存在集合 → 一次过滤 | 小 |
| **6** | ~~全量记忆硬上限实施~~ ✅ v2.2 | DB 查询层即执行头尾保留：前20条 + 最近200条，总上限220条；提示词层不再二次截断（v2.3消除双重截断） | 中 |

### 14.3 P2 — 功能增强

| # | 改动 | 说明 | 工作量 |
|---|------|------|--------|
| **7** | 对话摘要/分组展示 | 记忆列表顶部加摘要卡片（基于 AI 生成的 contactInsight + 最近话题关键词），按日期分组显示 | 大 |
| **8** | 长按收藏话术 | 浮窗中长按某条建议 → 收藏到该联系人的"常用话术库"，下次截图时优先参考 | 中 |
| **9** | 新手引导流程 | 首次进入 App 弹出引导："先创建一个联系人，就可以开始用悬浮球辅助聊天了"；空状态更醒目带动画 | 中 |
| **10** | 联系人卡片信息增强 | 列表显示记忆条数 + 最后一条消息预览（如"最后: 好的 明天见"）；按活跃度排序 | 中 |
| **11** | 使用统计面板 | 设置页轻量统计：总截图次数 / 总识别消息数 / Top3 活跃联系人 / 本周使用趋势 | 大 |

### 14.4 P3 — 体验打磨

| # | 改动 | 说明 | 工作量 |
|---|------|------|--------|
| **12** | 成功对勾过渡动画优化 | 对勾淡出 → 聊天图标淡入 的交叉渐变，或加微小弹跳动画 | 小 |
| **13** | "换一批"按钮位置/样式优化 | 从底部大按钮改为建议卡片右侧文字链，节省空间 | 小 |
| **14** | ~~浮窗倒计时 Timer 泄漏防护~~ ✅ v2.2 | `hideSuggestionOverlay()` 中确保调用 `stopCountdownTimer()` | 小 |
| **15** | ~~AppState listener dispose~~ ✅ v2.2 | `main.dart` 中注册的 listener 在 App 生命周期结束时 removeListener | 小 |
| **16** | 数据库版本号与 schema 同步 | 确认当前 schema 与 version 匹配，新增字段时递增版本号 | 小 |
| **17** | ~~UI文案全量中文化~~ ✅ v2.3 | 复制Toast、Scene标签等用户可见文本全部改为中文 | 小 |
| **18** | ~~System Prompt 静态缓存~~ ✅ v2.3 | ~3KB 提示词首次构建后缓存为 static 字段，避免每次调用重复拼接 | 小 |
| **19** | ~~截图文件名防碰撞~~ ✅ v2.3 | 文件名增加 UUID(8位) 前缀，解决快速连点时间戳碰撞问题 | 小 |
| **20** | ~~截图磁盘自动清理~~ ✅ v2.3 | 启动时清理过期截图，仅保留最近50张，防止磁盘膨胀 | 小 |
| **21** | ~~启动时记录恢复~~ ✅ v2.3 | 扫描 processing 状态记录并标记为 error，防止 App 被杀后数据脏乱 | 小 |
| **22** | ~~Java端前置校验+冷却~~ ✅ v2.3 | 截图前校验联系人选择 + 3秒冷却防止连点浪费 API 调用 | 小 |
| **23** | ~~设置页版本号同步~~ ✅ v2.4 | 版本号从 v2.0 更新为 v2.3，消除用户困惑 | 极小 |
| **24** | ~~Dart端死代码彻底清除~~ ✅ v2.4 | 移除 handleFloatingBallClick / captureScreenshot / _handleFloatingBallClick / _mapError 及 main.dart 旧入口 | 小 |
| **25** | ~~网络预检~~ ✅ v2.4 | AI调用前DNS查询火山引擎域名（3s超时），无网立即提示避免等180秒 | 中 |
| **26** | ~~异常分类处理~~ ✅ v2.4 | catch细分为SocketException/TimeoutException/FormatException/通用四类，提示精准化 | 中 |
| **27** | ~~记忆列表统一getAllMemories~~ ✅ v2.4 | 与DB层220条上限策略一致，消除limit硬编码不一致 | 小 |
| **28** | ~~换一批加载反馈~~ ✅ v2.4 | 刷新按钮禁用+半透明+标题"正在生成中..."，不立即关闭浮窗 | 中 |
| **29** | ~~清空记忆防误触升级~~ ✅ v2.4 | 红色边框OutlinedButton + 危险确认对话框 + 红色FilledButton | 小 |
| **30** | ~~启动IO并行化~~ ✅ v2.4 | recoverStuckProcessingRecords与cleanupOldScreenshots Future.wait并行 | 小 |
| **31** | ~~联系人列表防抖缩短~~ ✅ v2.4 | 防抖从30秒缩短至5秒，编辑后更快看到更新 | 极小 |
| **32** | ~~db封装增强~~ ✅ v2.4 | 新增getExistingSuggestionContents方法，platform_service不再直接SQL | 低 |
| **33** | ~~mkdirs返回值检查~~ ✅ v2.4 | 目录创建失败时Toast+球状态ERROR，防止静默失败 | 极小 |

### 14.5 P4 — 性能优化

| # | 改动 | 说明 | 工作量 |
|---|------|------|--------|
| **23** | 截图压缩预处理 | base64 编码前先将图片压缩至 max 1920px 宽度，减少内存占用（高清截图可能 5-10MB） | 中 |
| **24** | API Key 安全存储增强 | 当前使用 SharedPreferences 明文存储，考虑迁移至 Android Keystore / iOS Keychain | 中 |
| **25** | 联系人画像历史快照 | 当前 contactInsight 每次覆盖，无法回溯。新增 insights_history 表或 JSON 字段存储历史画像 | 大 |

### 14.6 版本规划建议

| 版本 | 目标 | 核心内容 |
|------|------|----------|
| **v2.2** ✅ | 体验优化 | 横向加载条 / 3秒自动回退 / 前置校验 / 联系人隔离 / 提示词去AI味 / 记忆限制 / 批量查重 |
| **v2.3** ✅ | 稳定性+代码健康 | 双重截断消除 / UI全量中文化 / Prompt缓存 / 死代码清除 / UUID防碰撞 / 磁盘清理 / 记录恢复 / 前置冷却 |
| **v2.4** ✅ | 深度优化 | 网络预检/异常分类/死代码彻底清除/换一批加载反馈/清空防误触/IO并行化/db封装/mkdirs检查 |
| **v2.5** | 状态同步+UI深度优化 | **状态同步修复/进度阶段提示/联系人标签/草稿入口增强/悬浮球保活/JSON容错升级/常量统一/协议版本协商 + 全面UI优化（动画/搜索/气泡/头像/日期分组/骨架屏等）** |
| **v2.6** | 数据洞察 | 使用统计面板 + 性能优化（截图压缩 / 安全存储 / 画像历史快照） |
| **v3.0** | 平台扩展 | iOS 支持 / 多语言 / 云端同步 / 团队协作模式 |

---

## 15. UI 体验优化路线图（v2.5）

> 本节基于 v2.4 版本代码的全面 UI 审查，涵盖过渡动画、视觉设计、交互体验、布局结构、缺失功能五个维度。

### 15.1 过渡动画（当前完全缺失）

| 场景 | 当前状态 | 优化方案 | 优先级 |
|------|---------|----------|--------|
| **Tab 切换** | `IndexedStack` 无动画，硬切 | 替换为 `AnimatedSwitcher` 或 `PageView`，加淡入+轻微位移(10-20px) | P1 |
| **页面跳转** | 默认 `MaterialPageRoute` 右滑入 | 可保留默认；联系人详情/草稿编辑可加 Hero 动画（头像→头像） | P2 |
| **联系人选中** | 即时变色，无过渡 | 加 `ScaleTransition`(0.95→1.0, 150ms) + 颜色渐变 | P1 |
| **删除操作** | 直接消失 | 加 `SlideTransition` 向左滑出(200ms) | P1 |
| **FAB 点击** | 仅 Material 涟漪 | 叠加 scale 动画(1.0→0.9→1.0, 100ms) | P2 |
| **浮窗出现/消失** | Java 端无动画 | 出现时 fade in + slide up(200ms)；消失时 fade out + slide down | P1 |
| **SnackBar** | 默认弹出 | 可保留默认 Material 行为 | — |

**技术方案**：
```dart
// Tab 切换动画示例
AnimatedSwitcher(
  duration: const Duration(milliseconds: 250),
  transitionBuilder: (child, animation) =>
      FadeTransition(opacity: animation, child: SlideTransition(
        position: Tween<Offset>(begin: const Offset(0, 0.05), end: Offset.zero)
            .animate(CurvedAnimation(parent: animation, curve: Curves.easeOut)),
        child: child,
      )),
  child: _pages[_currentIndex], // key 必须包含 index
)
```

### 15.2 视觉设计问题

#### 15.2.1 主题色彩系统偏单调

当前 `AppTheme` 仅定义 primary + secondary + error 三色，缺少语义色系：

| 缺失项 | 建议值 | 用途 |
|--------|--------|------|
| success 色 | `#4CAF50` | 成功状态、已保存确认 |
| warning 色 | `#FF9800` | 警告提示、待处理标记 |
| info 色 | `#2196F3` | 信息提示、场景标签背景 |
| surfaceVariant | `primaryContainer` 的低透明版 | 卡片层次区分 |
| 渐变头饰 | `LinearGradient(primary→secondary)` | 头像区、空状态背景、首屏欢迎卡片 |

#### 15.2.2 联系人头像过于简陋

**问题**：所有联系人仅用首字母 CircleAvatar + 固定纯色填充，首字母相同时无法区分。

**方案**：基于名字生成固定 hue 的 Pastel 色彩：

```dart
// 根据名字生成固定颜色
Color _nameColor(String name) {
  final hash = name.hashCode;
  final hue = (hash % 360).abs().toDouble();
  return HSLColor.fromAHSL(1.0, hue, 0.65, 0.85).toColor();
}
```

> 同一名字永远映射到同一颜色，重启 App 后不变。Pastel 低饱和度确保文字可读性。

#### 15.2.3 底部导航栏增强

| 缺失项 | 方案 |
|--------|------|
| 无 Badge 提示 | 草稿数量 Badge / 未处理截图数 Badge |
| 图标风格不统一 | 考虑自定义 SVG 图标或使用统一的 icon font |
| 缺少品牌感 | 选中态可加微渐变底色 |

#### 15.2.4 设置页信息密度过高

API Key 输入框与其他设置混在一起，视觉权重过高。
**方案**：将 API 配置收起到 `ExpansionTile` 或独立子页面。

### 15.3 交互体验问题

#### 15.3.1 联系人选择反馈不足

| 问题 | 方案 |
|------|------|
| 切换后用户可能没注意 AppBar 变化 | 切换时加短暂 SnackBar "已切换到 XXX" |
| 列表项选中态对比度不足（深色模式） | 加深选中背景色差（如 `primary.withOpacity(0.15)` vs `0.08`） |

#### 15.3.2 操作确认策略

| 操作 | 当前行为 | 优化建议 |
|------|----------|----------|
| 删除联系人 | 二次确认 Dialog ✅ | OK |
| **删除单条记忆** | **直接删除，无确认** ❌ | 改为 SnackBar + Undo 按钮（3秒内可撤回） |
| 清空全部记录 | 有二次确认 ✅ | OK |
| 长按联系人 | BottomSheet 弹出 ✅ | OK，但可加图标缩放动效 |

#### 15.3.3 草稿功能 UX 断裂

| 问题 | 方案 |
|------|------|
| 入口隐蔽 | ✅ 已在浮窗增加"存入草稿"按钮(v2.5) |
| 存入后不知道去哪找 | 存入成功后 SnackBar 带"查看草稿" action |
| 自动保存用户无感知 | dispose 时 autoSave 成功后显示轻量 Toast "已自动保存" |

#### 15.3.4 对话记录页(MemoryTimeline)体验问题

| 问题 | 当前 | 方案 | 优先级 |
|------|------|------|--------|
| 删除按钮(X)始终显示，易误触 | 右侧固定 X 按钮 | 改为**长按显示菜单**(复制/删除)，或**滑动删除** | P0 |
| 时间戳只有 HH:mm，跨天无法分辨 | 仅时间 | 加**日期分组标题**(今天/昨天/更早) | P1 |
| 无长按复制菜单 | 无 | 长按气泡弹出 BottomSheet：复制内容/删除/引用 | P1 |
| 搜索结果为空时提示不明显 | 小字提示 | 加空状态插图 + 引导文案 | P2 |
| 下拉刷新缺失 | 无 | 加 RefreshIndicator | P1 |

#### 15.3.5 新建/编辑联系人表单问题

| 问题 | 当前 | 方案 | 优先级 |
|------|------|------|--------|
| 相机图标是假按钮(点击无反应) | camera_alt 图标装饰 | **要么实现图片选择功能，要么移除图标** | P0 |
| 创意度 Slider 无实时预览 | 调了参数不知效果 | Slider 旁加示例文本预览区域 | P2 |
| 保存按钮仅在 AppBar | 不够醒目 | 底部也加一个固定"保存"按钮 | P2 |

### 15.4 布局与结构问题

#### 15.4.1 首屏信息冗余

```
AppBar: [无障碍] [悬浮球] [当前联系人Chip]
     ↓
"当前沟通对象: XXX"  ← 与上面 Chip 信息重复！
     ↓
联系人列表
     ↓
FAB [+]
```

**建议**：移除"当前沟通对象"文字行，或将其合并到 AppBar 的 Chip 中（Chip 已包含联系人名）。

#### 15.4.2 记录 Tab 空状态引导不足

未选联系人时记录 Tab 显示空状态，但 Tab 标题仍是"记录"，用户可能困惑为什么是空的。
**方案**：未选择联系人时该 Tab 显示引导 CTA → "请先在联系人中选择一位" + 跳转按钮。

#### 15.4.3 草稿列表信息密度低

当前只显示：内容预览(2行) + 联系人名 + 时间。
**建议增加**：
- 来源标记（手动创建 📝 / 从建议存入 💡）
- 字数统计
- 是否已使用过（复制过打 ✓）

### 15.5 缺失的 UI 功能

| 功能 | 重要性 | 说明 |
|------|--------|------|
| **联系人搜索** | **P0** | 超过 10 个联系人后列表滚动查找困难，需顶部 SearchBar |
| **骨架屏** | P1 | AI 处理中用 shimmer 骨架替代圆形 CircularProgressIndicator |
| **首次启动引导** | P1 | 新用户不知道要做什么（开启截图→选联系人→截图聊天），需 Onboarding Wizard |
| **下拉刷新(记录页)** | P1 | 记忆列表缺少 RefreshIndicator |
| **排序功能** | P2 | 联系人/草稿/记录均只有默认排序，无法按时间/名称/活跃度排序 |
| **批量操作** | P2 | 无法批量删除多条记忆 |
| **手势操作** | P2 | 列表项无左滑归档/右滑删除 |
| **Badge 徽标** | P2 | 草稿数量 / 未处理截图数 / 未读消息 |

### 15.6 实施优先级总览

#### Phase 1 — 立即实施（P0）

| # | 任务 | 文件 | 说明 |
|---|------|------|------|
| 1 | 联系人列表搜索栏 | `contacts_screen.dart` | 顶部 SearchBar，实时过滤 |
| 2 | 移除/实现相机图标 | `contacts_screen.dart` | 假按钮必须处理 |
| 3 | 记忆删除改 Undo | `memory_timeline_screen.dart` | SnackBar + action 撤回 |

#### Phase 2 — 核心体验提升（P1）

| # | 任务 | 文件 | 说明 |
|---|------|------|------|
| 4 | Tab 切换动画 | `home_screen.dart` | AnimatedSwitcher 替代 IndexedStack |
| 5 | 联系人头像差异化着色 | `contacts_screen.dart` | 名字→HSL Pastel 色彩 |
| 6 | 气泡消息长按菜单 | `memory_timeline_screen.dart` | 复制/删除 BottomSheet |
| 7 | 记录页日期分组 | `memory_timeline_screen.dart` | 今天/昨天/更早 分组标题 |
| 8 | 浮窗出现/消失动画 | `MainActivity.java` | Java 端 fade + slide |
| 9 | 记录页下拉刷新 | `memory_timeline_screen.dart` | RefreshIndicator |
| 10 | 联系人选中的 Scale 反馈 | `contacts_screen.dart` | 点击缩放动画 |

#### Phase 3 — 锦上添花（P2）

| # | 任务 | 说明 |
|---|------|------|
| 11 | 设置页 API 配置折叠 | ExpansionTile 收起 |
| 12 | FAB 扩展菜单 | SpeedDial（新建联系人/快速截图） |
| 13 | 深色模式细节优化 | 硬编码浅色适配修正 |
| 14 | Badge 徽标系统 | 草稿数/未处理数 |
| 15 | 骨架屏 | shimmer 效果替代 loading spinner |
| 16 | 首次启动引导 | Onboarding 3 步引导 |
| 17 | 草稿来源标记 | 手动/自动 标签 |
| 18 | 排序功能 | 联系人按活跃度排序 |
