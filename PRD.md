# Chat-Helper 产品需求文档（PRD）

> **版本**：v1.5
> **状态**：规划中
> **更新**：2026-06-05

---

## 目录

1. [产品概述](#1-产品概述)
2. [目标用户与典型场景](#2-目标用户与典型场景)
3. [双API引擎架构](#3-双api引擎架构)
4. [功能需求清单与优先级](#4-功能需求清单与优先级)
5. [功能详述](#5-功能详述)
   - [5.1 截图获取](#51-截图获取)
   - [5.2 悬浮球状态反馈](#52-悬浮球状态反馈)
   - [5.3 全局浮窗建议卡片](#53-全局浮窗建议卡片)
   - [5.4 AI-1：豆包Seed Vision 对话提取](#54-ai-1豆包seed-vision-对话提取)
   - [5.5 AI-2：DeepSeek 回复建议生成](#55-ai-2deepseek-回复建议生成)
   - [5.6 联系人档案管理](#56-联系人档案管理)
   - [5.7 话术草稿编辑器](#57-话术草稿编辑器)
   - [5.8 历史对话记录](#58-历史对话记录)
   - [5.9 设置与参数配置](#59-设置与参数配置)
6. [交互流程总览](#6-交互流程总览)
7. [UI 设计方向](#7-ui-设计方向)
8. [数据模型](#8-数据模型)
9. [技术可行性评估](#9-技术可行性评估)
10. [API 集成规范](#10-api-集成规范)

---

## 1. 产品概述

### 1.1 一句话定位

**Chat-Helper** 是一款"常驻桌面、一键截图、AI 参谋"的智能聊天辅助工具——点一下悬浮球，豆包提取对话 + DeepSeek 生成回复建议，直接浮在聊天 App 上方，看一眼就知道怎么回。

### 1.2 核心价值主张

| 痛点 | 解决方案 |
|------|----------|
| 聊天冷场、不知道回什么 | AI 先给方向思路，再给具体话术 |
| 回复像机器人、不自然 | Prompt 三重强调"说人话"，禁止 AI 腔 |
| 切换 App 麻烦 | 点击悬浮球→浮窗直接出现在聊天App上方，无需切回 |
| 点了不知道有没有反应 | 悬浮球即时动画反馈（处理中→成功/失败） |
| 多联系人记不住上下文 | 每位联系人独立记忆库，自动调取历史 |

---

## 2. 目标用户与典型场景

### 2.1 核心场景

**场景**：微信收到消息，不知如何回应
1. 打开微信看到消息
2. 点击悬浮球 → 悬浮球立即变为"处理中"旋转动画
3. 几秒后，微信界面上方弹出一张浮窗卡片——"对话思路"+3条具体话术
4. 看思路、选话术 → 点复制 → 贴回微信 → 浮窗自动消失
5. **全程不需要切回 Chat-Helper App**

---

## 3. 双API引擎架构

### 3.1 架构图

```
┌──────────────────────────────────────────────────┐
│                  用户点击悬浮球                     │
│          悬浮球立即变为"处理中"旋转动画               │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│           截图：AccessibilityService               │
│           takeScreenshot() 无感截图                │
│           不弹任何对话框                            │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│  加载当前联系人最近 6 条记忆（去重基准）              │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│         AI-1：豆包Seed Vision（云端）              │
│  输入：base64截图 + 已知历史(6条) + System Prompt   │
│  输出：新增 messages[]（含 time 字段）             │
│  模型：Doubao-Seed-1-6-vision                     │
│  Base URL：ark.cn-beijing.volces.com/api/v3       │
│              ↓                                    │
│  仅新增消息 → 落库 chat_memories 表               │
│  （全量重复则不入库、不触发 AI-2）                  │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│  重新加载完整历史（含时间戳）+ 注入当前时间           │
│  计算时间间隔 → 判断场景(A/B/C)                    │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│         AI-2：DeepSeek（云端）                     │
│  输入：system + 历史messages[](带timestamp)        │
│       + current_time + time_elapsed + scene       │
│  输出：scene + direction + 3条建议(suggestions)    │
│  模型：deepseek-chat                              │
│  Base URL：api.deepseek.com                       │
│  无状态API（客户端自行拼接 messages[]）             │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│   全局浮窗卡片弹出（WindowManager，聊天App上方）     │
│   场景标签 + 💡对话思路 + 3条具体话术               │
│   悬浮球绿色闪光 → 恢复空闲 → 浮窗N秒后自动消失      │
└──────────────────────────────────────────────────┘
```

### 3.2 两个AI角色

| 维度 | AI-1 对话提取 | AI-2 回复建议 |
|------|-------------|-------------|
| **提供商** | 火山引擎 豆包Seed Vision | DeepSeek |
| **API** | `POST /chat/completions` | `POST /chat/completions` |
| **Base URL** | `https://ark.cn-beijing.volces.com/api/v3` | `https://api.deepseek.com` |
| **推荐模型** | `Doubao-Seed-1-6-vision` | `deepseek-chat` |
| **图片上传** | 是（仅截图） | 否（仅纯文本） |
| **延迟** | 2-5s | 3-8s |
| **状态** | 无状态 | 无状态（自行拼接messages[]） |

---

## 4. 功能需求清单与优先级

### 4.1 优先级定义

| 级别 | 含义 |
|------|------|
| **P0** | 核心必做，MVP必须上线 |
| **P1** | 重要功能，早期版本上线 |
| **P2** | 锦上添花，迭代中加入 |

### 4.2 功能全景

| 模块 | 功能点 | 优先级 | 备注 |
|------|--------|--------|------|
| **截图** | AccessibilityService 无感截图 | P0 | |
| **悬浮球反馈** | 状态动画（处理中/成功/失败） | P0 | |
| | 防重复点击保护 | P0 | |
| **AI-1** | 豆包Seed Vision 对话提取 | P0 | |
| | 增量去重（最近6条记忆比对） | P0 | v1.5 新增 |
| | 时间戳输出 | P0 | v1.5 新增 |
| | 提取结果自动落库（仅新增） | P0 | |
| **AI-2** | DeepSeek 回复建议生成 | P0 | |
| | 三大场景自动判断(A/B/C) | P0 | v1.5 新增 |
| | 对话方向(direction) + 3条建议 | P0 | |
| | 方向铁律（防回复角色反转） | P0 | v1.5 新增 |
| | 时间上下文感知（时间戳+间隔） | P0 | v1.5 新增 |
| | 提示词强调"说人话" | P0 | |
| | 每条建议附带理由 | P1 | |
| | 换一批 | P1 | |
| **浮窗卡片** | 全局浮窗（聊天App上方弹出） | P0 | |
| | 自动消失（可设置时间） | P0 | |
| | 一键复制 | P0 | |
| | 复制后延期消失 | P1 | |
| **联系人** | 新建/编辑/删除 | P0 | |
| | 列表/搜索/快速切换 | P0 | |
| **记忆库** | 对话自动保存 | P0 | |
| | 历史时间线查看 | P0 | |
| | 关键词搜索 | P1 | |
| **设置** | 豆包API Key | P0 | |
| | DeepSeek API Key | P0 | |
| | 浮窗显示时间 | P0 | 5s/8s/15s/手动 |
| | 深色模式 | P1 | |

---

## 5. 功能详述

### 5.1 截图获取

Android `AccessibilityService.takeScreenshot()`（Android 11+）。

> **不弹"录制屏幕"对话框。** 首次去"设置→无障碍"开启服务后，之后完全无感。

### 5.2 悬浮球状态反馈

点击悬浮球后必须有即时反馈，用户不能疑惑"到底点没点上"。

#### 5.2.1 状态定义

```

  空闲              处理中              成功              失败
 ┌─────┐          ┌─────────┐        ┌─────────┐      ┌─────────┐
 │ 💬  │ 点击→    │ ⟳  💬  │ 完成→  │ ✓  💬  │      │ ✗  💬  │
 │     │          │ 脉冲旋转  │        │ 绿色闪光  │      │ 红色闪烁  │
 └─────┘          └─────────┘        └─────────┘      └─────────┘
 alpha 0.8         alpha 1.0         1秒后恢复         1秒后恢复
 可点击            不可点击           不可点击           可点击
 可拖拽            不可拖拽           可拖拽             可拖拽
```

| 状态 | 视觉 | 动画 | 交互 |
|------|------|------|------|
| **空闲** | 静态图标，alpha 0.8 | 无 | 可点击、可拖拽 |
| **处理中** | 图标替换为loading，alpha 1.0 | 脉冲光晕旋转 + 外环进度环 | 不可点击（防重复） |
| **成功** | 绿色对勾覆盖 | 绿色闪光 1秒 | 不可点击 |
| **失败** | 红色叉号覆盖 | 红色闪烁 + 微弱振动 | 不可点击 |

#### 5.2.2 技术实现

Java 侧 `ACTION_UP` 检测到点击且非拖拽后：
1. 立即 `setImageResource(R.drawable.ball_processing)` + `setEnabled(false)` — **即时反馈**
2. 异步调用 `triggerScreenshot()` → Dart 流水线
3. 完成后 MethodChannel 回调 → 恢复图标 + 绿色/红色动画
4. `setEnabled(true)` + 恢复原图标

### 5.3 全局浮窗建议卡片

> **核心原则：建议卡片必须直接浮在聊天 App 上方，用户不需要切回 Chat-Helper。**

#### 5.3.1 技术方案

Java `WindowManager.addView()` 添加一个原生 View 浮窗：
- `TYPE_APPLICATION_OVERLAY` — 悬浮在所有 App 之上
- `FLAG_NOT_FOCUSABLE` — 不抢输入焦点
- `FLAG_NOT_TOUCH_MODAL` — 点击外部继续传递事件

#### 5.3.2 布局设计

```
┌──────────────────────────────────────────┐
│ 💡 对话思路                  [⚡8s] [✕] │ ← 标题栏 + 倒计时
├──────────────────────────────────────────┤
│  顺着对方的话题接，表现出你也想去，        │ ← Part1: 方向（蓝色底）
│  但加点俏皮感，别太急切                    │
├──────────────────────────────────────────┤
│  热情  好呀！上次去还是秋天呢              │ ← Part2: 具体话术
│        花开了肯定好看 🌸              │    [复制]
│        理由：积极回应 + 回忆共鸣            │
├──────────────────────────────────────────┤
│  幽默  这是在约我吗哈哈 去哪去哪            │
│        理由：轻松调侃，制造对话节奏          │
├──────────────────────────────────────────┤
│  稳妥  行，几点？我收拾一下                │
│        理由：简洁直接，不拖不带压力          │
├──────────────────────────────────────────┤
│          [换一批]               [关闭]    │
└──────────────────────────────────────────┘
```

| 区域 | 内容 | 说明 |
|------|------|------|
| **标题栏** | 💡 对话思路 + 倒计时 + ✕ | 倒计时实时更新（e.g. "8s"→"7s"→...） |
| **Part1 方向** | 模糊对话方向（1句话） | 蓝色浅底，15sp |
| **Part2 话术** | 3条具体话术（风格+内容+理由） | 每条可独立复制 |

#### 5.3.3 生命周期

| 事件 | 行为 |
|------|------|
| **出现** | AI-2 返回结果后，从底部滑入，300ms |
| **倒计时** | 默认 8 秒倒计时（可在设置中调整） |
| **点击复制** | 呼出 Toast "已复制"，倒计时重置+3秒延期 |
| **倒计时到0** | 浮窗淡出消失 |
| **点击 ✕** | 立即关闭 |
| **点击外部区域** | 无反应（避免误触） |
| **点击「换一批」** | 浮窗内 loading → 新建议替换 |
| **点击「关闭」** | 立即关闭 |

#### 5.3.4 设置项

| 配置项 | 选项 | 默认值 |
|--------|------|--------|
| **建议卡显示时间** | 5秒 / 8秒 / 15秒 / 手动关闭 | **8秒** |

### 5.4 AI-1：豆包Seed Vision 对话提取

> **适用范围**：仅两人私聊场景，不处理群聊。

#### 5.4.1 调用流程（含增量去重）

```
截图PNG → Base64编码
       │
       ▼
从 chat_memories 加载当前联系人最近 6 条记忆（作为去重基准）
       │
       ▼
POST https://ark.cn-beijing.volces.com/api/v3/chat/completions
  Authorization: Bearer <豆包API Key>
  Content-Type: application/json
  {
    "model": "Doubao-Seed-1-6-vision",
    "messages": [{
      "role": "user",
      "content": [
        {"type": "text", "text": "<SYSTEM_PROMPT + 已知历史 + 增量规则>"},
        {"type": "image_url", "image_url": {"url": "data:image/png;base64,..."}}
      ]
    }]
  }
       │
       ▼
解析返回JSON → 仅将标记为新增的消息落入 chat_memories 表
       │
       ▼
触发 AI-2（传入完整历史含时间戳）
```

#### 5.4.2 System Prompt（v1.5 增量识别版）

```
你是一个聊天记录解析器。图片是一个手机【两人私聊】窗口的截图。

【你的任务】
识别截图中的对话消息，与已知历史比对后，仅输出新增消息。

【已知历史 — 以下消息已存在于记忆库，请勿重复输出】
{last_6_memories_json_array}
示例格式：
[
  {"speaker": "partner", "content": "在吗", "time": "2026-06-04T10:00:00"},
  {"speaker": "me", "content": "在呀", "time": "2026-06-04T10:01:00"},
  ...
]

【角色判定规则 — 两人私聊】
这是一个两人私聊窗口，只有两个说话人：
1. 屏幕右半区气泡 → me（手机持有者 / 截图者 / 需要建议的一方）
2. 屏幕左半区气泡 → partner（对话对象 / 与 me 聊天的人）
3. 辅助确认：右侧气泡通常带绿色/蓝色背景色 = me
4. 辅助确认：左侧气泡通常带白色/灰色背景色 = partner
5. me 的头像（如有）出现在右侧或不可见；partner 的头像出现在左侧

【增量输出规则】
- 只输出上述"已知历史"中不存在的新增消息
- 判断重复标准：speaker 相同 + content 高度相似（允许表情/标点的微小差异）
- 如果截图中的所有消息都已存在于历史中 → 输出空 messages 数组 []
- 每条消息必须包含 time 字段（从截图中估算的时间，格式见下方）

【输出格式】严格JSON：
{
  "messages": [
    {"speaker": "me|partner", "content": "消息内容", "y": 垂直像素位置, "time": "ISO8601或相对时间"}
  ]
}

其他规则：
1. y 为该消息在截图中的垂直像素坐标，按 y 从小到大排列
2. 过滤系统消息（撤回提示、时间戳分隔线、状态栏等）
3. emoji 保留原样
4. 空白无效区域不输出
5. time 字段优先使用截图中可见的时间戳；如无法精确识别，
   请根据消息在截图中的相对位置估算一个合理时间（使用 ISO8601 格式）
```

| 参数 | 值 |
|------|-----|
| `model` | `Doubao-Seed-1-6-vision` |
| `temperature` | 0.1 |
| `max_tokens` | 2048 |

#### 5.4.3 去重机制详解

| 配置项 | 值 | 说明 |
|--------|-----|------|
| **去重基准数量** | **最近 6 条** | 从 `chat_memories` 按 `created_at DESC` 取最近 6 条，倒序后传给 AI-1 |
| **去重判断方** | **AI-1（豆包Vision）** | 利用多模态模型的语义理解能力做内容比对，优于客户端文本匹配 |
| **落库策略** | **仅新增** | 客户端收到 AI-1 返回后，只将 messages 数组中的条目插入 `chat_memories` |
| **全量重复时的行为** | **不入库 + 不触发 AI-2** | 如果返回空数组，提示用户"未检测到新消息"，不浪费 AI-2 调用 |

#### 5.4.4 触发场景说明

AI-1 需要正确处理以下三种截图触发场景：

| 场景 | 描述 | AI-1 预期行为 |
|------|------|---------------|
| **A. 对方发了新消息** | partner 最后发了一条/几条，me 尚未回复 | 识别出 partner 的新消息，标记为新增 |
| **B. 跨天主动发起** | 最后一次聊天是几小时/几天前，me 想主动开启新话题 | 截图中可能无新增消息（历史已全部入库），返回空数组 |
| **C. 我追加消息** | me 刚发了一条对方没回，me 想继续说 | 识别出 me 的最新消息，标记为新增 |

> **注意**：无论哪种场景，AI-1 只负责"提取+去重"，不做任何回复判断。回复策略完全由 AI-2 根据完整上下文决定。

### 5.5 AI-2：DeepSeek 回复建议生成

> **核心能力**：根据完整对话历史（含时间戳）+ 当前时间，自动判断对话场景，生成精准的回复建议。

#### 5.5.1 调用流程（含时间上下文）

```
AI-1 返回的新增消息 → 落库后重新加载完整历史（含时间戳）
       │
       ▼
构建 messages[]（每条带 timestamp 字段） + 注入当前时间
       │
       ▼
POST https://api.deepseek.com/chat/completions
  Authorization: Bearer <DeepSeek API Key>
  {
    "model": "deepseek-chat",
    "messages": [
      {"role": "system", "content": "<SYSTEM_PROMPT 含场景判断规则>"},
      {"role": "user", "content": "...", "timestamp": "2026-06-04T10:00:00"},
      {"role": "assistant", "content": "...", "timestamp": "2026-06-04T10:01:00"},
      ...
    ]
  }
       │
       ▼
解析返回 JSON → direction + suggestions → 浮窗展示
```

#### 5.5.2 System Prompt（v1.5 场景感知版）

```
你是一个高情商社交助手。你不是机器人，你是真正懂得人情世故的朋友。

【你的任务】
帮助 me（截图者 / 手机持有者）决定接下来对 partner（对话对象）说什么。
me 现在打开了聊天窗口，正在看截图中的对话，需要你的建议。

【对话上下文 — 含时间戳的完整历史】
以下是按时间顺序排列的全部对话记录：
{messages_with_timestamps}

【关键时间信息】
- 现在（用户截图的时刻）：{current_time}
- 最后一条消息的时间：{last_msg_time}
- 距最后一条消息已过去：{time_elapsed}

【三大场景 — 请自动判断并调整策略】

请根据"距最后一条消息的时间间隔"和"最后一条消息的说话人"，自动判断当前属于哪种场景：

┌─────────────┬──────────────────────────┬──────────────────────────────┐
│ 场景         │ 判断条件                  │ me 的意图 & AI 应该做什么     │
├─────────────┼──────────────────────────┼──────────────────────────────┤
│ A. 待回复    │ 最后一条是 partner 发的，   │ me 需要回复 partner 的最新    │
│             │ 且时间间隔较短              │ 消息。建议应该是：回应内容、    │
│             │（几分钟~几小时）            │ 延续话题、表达态度             │
├─────────────┼──────────────────────────┼──────────────────────────────┤
│ B. 主动发起  │ 距最后一条消息已超过数小时   │ me 是主动想找对方聊天。        │
│             │ 甚至隔天                   │ 建议应该是：自然开场、提起话题、  │
│             │                          │ 轻松破冰。不要假装在"回复"某条   │
│             │                          │ 具体消息                       │
├─────────────┼──────────────────────────┼──────────────────────────────┤
│ C. 延续/追加 │ 最后一条是 me 发的          │ me 发了一条对方还没回，但 me   │
│             │（对方尚未回复），或者 me     │ 想继续说下去。建议应该是：      │
│             │ 想追加点什么               │ 补充说明、转换话题、追问、       │
│             │                          │ 或者轻松收尾                    │
└─────────────┴──────────────────────────┴──────────────────────────────┘

【方向铁律 — 绝对不能混淆】

你生成的每一条 suggestion，都是 me 即将发给 partner 的话。
me 是发消息的人，不是等消息的人。

方向自检清单：
- 如果你的建议看起来像"好的呀""收到""哈哈哈""没问题"这种被动回应式语句，
  → 方向可能反了！那通常是 partner 该说的话
- 如果你的建议看起来像是对方在回你（比如"好的那我等你""哈哈是的"这种确认式语气），
  → 方向反了！那是 partner 回 me 的话
- me 的回复应该是：主动回应、延续话题、提出问题、表达态度、开启新话题
- partner 的回复才会是：确认收到、被动等待、回应 me 的提问

正确示例（me 发给 partner）：
- "好呀！上次去还是秋天呢，花开了肯定好看" → 主动回应 + 回忆共鸣
- "这是在约我吗哈哈，去哪去哪" → 主动调侃
- "行，几点？我收拾一下" → 主动推进计划

错误示例（这看起来像 partner 回 me 的 — 不要输出这类）：
- "好哒，那我等你哦" ← 被动等待
- "哈哈哈是啊" ← 单纯附和，没有推动对话

【说人话铁律】
1. 说人话。说人话。说人话。不要用"当然可以""很高兴为你""综上所述"这种AI腔
2. 不要用破折号、分号、过于工整的排比句。正常人发微信不会那样说话
3. 用词口语化，像跟朋友发微信一样自然。允许语气词：哈哈、嘛、呀、啦、呢
4. 每条话术 1-3 句话，不要长篇大论
5. 回复语气: {tone}  回复长度: {length}  创意度: {creativity}/10
6. 3条建议的风格要不同（从热情/幽默/稳妥/直白/温柔 中选）
7. direction 是给用户一个思路，帮助 ta 理解当前该往哪个方向回
8. 已有的emoji可以保留，但不要生硬添加

【输出格式】严格JSON（不要包含其他文字）：
{
  "scene": "A|B|C",
  "scene_description": "一句话描述当前场景",
  "direction": "用一句话说清这轮对话怎么回最合适——给方向和思路",
  "suggestions": [
    {"style": "风格名", "content": "具体回复话术（me发给partner的话）", "reason": "简短理由"}
  ]
}

示例输出（场景A - 待回复）：
{
  "scene": "A",
  "scene_description": "partner 刚发了消息，me 需要回复",
  "direction": "顺着对方的话题接，表现出你也想去，但加点俏皮感别太急切",
  "suggestions": [
    {"style": "热情", "content": "好呀！上次去还是秋天呢 花开了肯定好看", "reason": "积极回应 + 回忆共鸣"},
    {"style": "幽默", "content": "这是在约我吗哈哈 去哪去哪", "reason": "轻松调侃，制造对话节奏"},
    {"style": "稳妥", "content": "行，几点？我收拾一下", "reason": "简洁直接，不拖不带压力"}
  ]
}

示例输出（场景B - 主动发起）：
{
  "scene": "B",
  "scene_description": "距离上次聊天已经很久了，me 主动破冰",
  "direction": "用一个轻松的话题自然重启对话，不要太正式",
  "suggestions": [
    {"style": "随意", "content": "刚看到个事突然想到你，你最近咋样", "reason": "自然提起，不给压力"},
    {"style": "具体", "content": "对了，上次说的那个展览好像这周末最后一天", "reason": "借助之前话题自然衔接"},
    {"style": "幽默", "content": "失踪人口回归！在吗在吗", "reason": "轻松破冰，适合关系较熟的人"}
  ]
}

示例输出（场景C - 追加/延续）：
{
  "scene": "C",
  "scene_description": "me 刚发了一条，对方还没回，me 想继续说",
  "direction": "补充说明或转换话题，不等待对方回复",
  "suggestions": [
    {"style": "补充", "content": "哦对了还有一件事忘了说", "reason": "自然追加信息"},
    {"style": "追问", "content": "你觉得呢？我还是有点纠结", "reason": "延续话题并邀请参与"},
    {"style": "收转", "content": "不说这个了，问你个事", "reason": "优雅转换话题"}
  ]
}
```

#### 5.5.3 输出结构变化

| | v1.4 | v1.5 |
|------|------|------|
| **输出内容** | direction + 3条suggestions | **scene + scene_description + direction + 3条suggestions** |
| **Prompt管控** | 三重"说人话"+禁止模式 | **+ 三大场景自动判断 + 方向铁律自检** |
| **时间感知** | 无 | **每条消息带时间戳 + 注入当前时间 + 时间间隔计算** |
| **方向保证** | 隐含要求 | **显式铁律 + 正误示例 + 自检清单** |

#### 5.5.4 消息映射规则

| 记忆库 speaker | DeepSeek role | 说明 |
|---------------|---------------|------|
| `partner` | `user` | 对方说的话 = 用户需要回应的内容 |
| `me` | `assistant` | me 说过的话 = 已有的回复历史 |

**每条 message 必须携带 `timestamp` 字段**，格式为 ISO8601（如 `2026-06-04T10:00:00`）。如果 AI-1 无法提供精确时间戳，使用消息的 `created_at` 数据库时间作为 fallback。

#### 5.5.5 客户端注入的上下文变量

AI-2 调用时，除 messages[] 外还需传入以下变量供 Prompt 填充：

| 变量 | 来源 | 示例值 |
|------|------|--------|
| `{current_time}` | `DateTime.now().toIso8601String()` | `2026-06-05T14:30:00` |
| `{last_msg_time}` | 历史消息中最后一条的 timestamp | `2026-06-05T10:15:00` |
| `{time_elapsed}` | current_time - last_msg_time 的人类可读描述 | `4小时15分钟` 或 `昨天晚上` |

客户端需实现一个辅助函数将时间差转为友好描述：

```
< 1小时    → "N分钟前"
1-24小时   → "N小时前"
1-7天      → "N天前"
>= 7天     → "YYYY-MM-DD"
```

#### 5.5.6 个性化参数（从联系人档案读取）

| 参数 | 可选值 | 默认值 |
|------|--------|--------|
| `tone` | 轻松随意/正式得体/幽默风趣/温柔体贴/自然中性 | 自然中性 |
| `length` | 一句话/两三句/详细展开 | 两三句 |
| `creativity` | 0.0-1.0 | 0.5 |

---

### 5.6 联系人档案管理

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 姓名 | String | 是 | |
| 关系分类 | Enum | 否 | 朋友/同事/家人/暧昧/自定义 |
| 语气偏好 | Enum | 否 | 默认"自然中性" |
| 长度偏好 | Enum | 否 | 默认"两三句" |
| 创意度偏好 | Float | 否 | 默认0.5 |

- 首页联系人Tab → 列表/搜索/快速切换
- 选中联系人高亮

---

### 5.7 话术草稿编辑器

- 从浮窗卡片点击进入
- 预填AI建议文本
- 自由修改 → 一键复制/保存

---

### 5.8 历史对话记录

- 按联系人筛选，时间线视图
- 时间倒序 + 关键词搜索
- 单条/批量删除

---

### 5.9 设置与参数配置

#### 5.9.1 AI-1 豆包Seed Vision 配置

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| **豆包API Key** | console.volcengine.com 获取 | - |
| **AI-1模型** | | `Doubao-Seed-1-6-vision` |
| Base URL（固定） | | `https://ark.cn-beijing.volces.com/api/v3` |

#### 5.9.2 AI-2 DeepSeek 配置

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| **DeepSeek API Key** | platform.deepseek.com 获取 | - |
| **AI-2模型** | | `deepseek-chat` |
| 默认语气 | 全局默认 | 自然中性 |
| 默认长度 | 全局默认 | 两三句 |
| 默认创意度 | 全局默认 | 0.5 |

#### 5.9.3 显示设置

| 配置项 | 说明 | 选项 | 默认值 |
|--------|------|------|--------|
| **建议卡显示时间** | 浮窗自动消失时间 | 5秒/8秒/15秒/手动关闭 | **8秒** |

#### 5.9.4 外观

- 浅色/深色模式

---

## 6. 交互流程总览

### 6.1 首次使用（一次性）

```
① 设置→无障碍→已安装的服务→开启 Chat-Helper
② 打开 App → 设置页 → 配置豆包API Key + DeepSeek API Key
③ 首页 → 创建联系人 → 选中
④ AppBar 点 ☰ → 开启悬浮球
⑤ 设置浮窗显示时间（默认8秒）
```

### 6.2 日常使用（支持三种触发场景）

**场景A — 对方发了消息，我要回复**（最常见）：
```
在聊天App看到 partner 的新消息
       │
       ▼
点击悬浮球 → 【处理中】旋转动画 ⏳
       │
       ▼
截图 → 豆包Vision提取(与最近6条比对) → 仅新增落库
       │
       ▼
DeepSeek 判定场景A → "partner刚发了消息，me需要回复"
       │
       ▼
┌──────────────────────────────────────┐
│ [A 待回复] � 顺着对方话题接... [8s] ✕│ ← 浮窗出现在聊天App上方
│  ────────────────────────────────── │
│  热情  好呀！花开了肯定好看         │
│  幽默  这是在约我吗哈哈             │
│  稳妥  行，几点？                   │
│          [换一批]           [关闭]  │
└──────────────────────────────────────┘
```

**场景B — 跨天/隔了很久，我想主动找对方聊**：
```
打开很久没联系的聊天窗口
       │
       ▼
点击悬浮球 → 【处理中】旋转动画 ⏳
       │
       ▼
截图 → 豆包Vision提取 → 与历史比对 → 无新增（或仅少量）
       │
       ▼
DeepSeek 判定场景B → "距离上次聊天已很久，me主动破冰"
       │
       ▼
┌──────────────────────────────────────┐
│ [B 主动发起] 💬 自然重启对话... [8s] ✕│
│  ────────────────────────────────── │
│  随意  刚看到个事突然想到你          │
│  具体  对了上次说的展览这周末最后一天  │
│  幽默  失踪人口回归！在吗在吗        │
│          [换一批]           [关闭]  │
└──────────────────────────────────────┘
```

**场景C — 我刚发了一条对方没回，想继续说**：
```
我刚发了一条消息，对方还没回，但我想追加
       │
       ▼
点击悬浮球 → 【处理中】旋转动画 ⏳
       │
       ▼
截图 → 豆包Vision提取 → 新增 me 的最新消息
       │
       ▼
DeepSeek 判定场景C → "me刚发了一条，对方还没回，me想继续说"
       │
       ▼
┌──────────────────────────────────────┐
│ [C 追加延续] 💬 补充或转换话题...[8s]✕│
│  ────────────────────────────────── │
│  补充  哦对了还有一件事忘了说         │
│  追问  你觉得呢？我还是有点纠结       │
│  收转  不说这个了，问你个事           │
│          [换一批]           [关闭]  │
└──────────────────────────────────────┘
```

> **通用后续操作**：点"复制" → Toast "已复制" → 贴回微信 → 浮窗延期3秒后消失 / 等待倒计时自动消失 → 悬浮球绿色闪光 ✓

### 6.3 端到端耗时

| 步骤 | 耗时 |
|------|------|
| 截图 | <0.5s |
| 豆包Vision | 2-5s |
| DeepSeek | 3-8s |
| **总计** | **5-14s** |

---

## 7. UI 设计方向

- **悬浮球**：圆形48dp，半透明，有状态动画
- **浮窗卡片**：底部弹出，圆角24dp，半透明黑色蒙层，8秒后自动消失
- **深色模式**支持

---

## 8. 数据模型

```sql
CREATE TABLE contacts (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    relationship TEXT,
    gender TEXT,
    notes TEXT,
    tone_preference TEXT DEFAULT 'neutral',
    length_preference TEXT DEFAULT 'medium',
    creativity_preference REAL DEFAULT 0.5,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    last_active_at INTEGER
);

CREATE TABLE chat_memories (
    id TEXT PRIMARY KEY,
    contact_id TEXT NOT NULL,
    screenshot_id TEXT,
    speaker TEXT NOT NULL CHECK(speaker IN ('me', 'partner')),
    content TEXT NOT NULL,
    created_at INTEGER NOT NULL,
    FOREIGN KEY (contact_id) REFERENCES contacts(id) ON DELETE CASCADE
);
CREATE INDEX idx_memories_contact ON chat_memories(contact_id, created_at);

CREATE TABLE screenshots (
    id TEXT PRIMARY KEY,
    contact_id TEXT,
    file_path TEXT NOT NULL,
    ai1_result_json TEXT,
    ai1_status TEXT DEFAULT 'pending',
    created_at INTEGER NOT NULL
);

CREATE TABLE suggestions (
    id TEXT PRIMARY KEY,
    contact_id TEXT NOT NULL,
    screenshot_id TEXT,
    direction TEXT,
    style TEXT,
    content TEXT NOT NULL,
    reason TEXT,
    is_favorited INTEGER DEFAULT 0,
    is_copied INTEGER DEFAULT 0,
    created_at INTEGER NOT NULL
);
```

> `suggestions` 表新增 `direction` 字段 — 存储 AI-2 返回的对话方向

---

## 9. 技术可行性评估

| 层级 | 技术 |
|------|------|
| **截图** | AccessibilityService.takeScreenshot() |
| **AI-1** | 豆包Seed Vision (Doubao-Seed-1-6-vision) |
| **AI-2** | DeepSeek (deepseek-chat) |
| **HTTP** | Dart `http` package |
| **存储** | SQLite + SharedPreferences + FlutterSecureStorage |
| **悬浮球** | Android WindowManager (Java) |
| **浮窗卡片** | Android WindowManager 原生 View (Java) |
| **UI** | Flutter + Provider |

### 权限

| 权限 | 用途 |
|------|------|
| `SYSTEM_ALERT_WINDOW` | 悬浮球 + 浮窗卡片 |
| `BIND_ACCESSIBILITY_SERVICE` | 无感截图 |
| `FOREGROUND_SERVICE` | 保活 |
| `INTERNET` | 豆包 + DeepSeek |

---

## 10. API 集成规范

### 10.1 AI-1 豆包Seed Vision

```http
POST https://ark.cn-beijing.volces.com/api/v3/chat/completions
Authorization: Bearer <豆包API Key>
Content-Type: application/json

{
  "model": "Doubao-Seed-1-6-vision",
  "messages": [
    {
      "role": "user",
      "content": [
        {"type": "text", "text": "你是一个聊天记录解析器。图片是一个手机聊天截图。\n\n请识别截图中的所有对话消息，按时间顺序（从上到下）提取，输出严格JSON：\n\n{\n  \"messages\": [\n    {\"speaker\": \"me\", \"content\": \"消息内容\", \"y\": 垂直像素位置},\n    {\"speaker\": \"partner\", \"content\": \"消息内容\", \"y\": 垂直像素位置}\n  ]\n}\n\n规则：\n1. speaker：右侧气泡=me，左侧气泡=partner\n2. 按y从小到大排列\n3. 过滤系统消息\n4. 群聊昵称只保留内容\n5. emoji保留"},
        {"type": "image_url", "image_url": {"url": "data:image/png;base64,{base64}"}}
      ]
    }
  ],
  "temperature": 0.1,
  "max_tokens": 2048
}
```

### 10.2 AI-2 DeepSeek

```http
POST https://api.deepseek.com/chat/completions
Authorization: Bearer <DeepSeek API Key>
Content-Type: application/json

{
  "model": "deepseek-chat",
  "messages": [
    {
      "role": "system",
      "content": "你是一个高情商社交助手。你不是机器人，你是一个真正懂得人情世故的朋友。\n\n根据聊天历史，帮用户想清楚怎么回复。\n\n输出严格JSON（不要包含其他文字）：\n{\n  \"direction\": \"用一句话说清这轮对话怎么回最合适\",\n  \"suggestions\": [\n    {\"style\": \"风格名\", \"content\": \"具体回复话术\", \"reason\": \"简短理由\"}\n  ]\n}\n\n铁律：\n1. 说人话。说人话。说人话。不要用\"当然可以\"\"很高兴为你\"\"综上所述\"\n2. 不用破折号、分号、工整排比句。正常人发微信不会那样\n3. 口语化，像跟朋友聊天。允许语气词：哈哈、嘛、呀、啦、呢\n4. 每条1-3句话\n5. 语气:{tone} 长度:{length} 创意度:{creativity}/10\n6. 3条风格不同（热情/幽默/稳妥/直白/温柔）\n7. direction给思路，比如\"顺着情绪共鸣\"\"制造悬念反问\"\n8. emoji保留但不要生硬添加"
    },
    {"role": "user", "content": "今天天气真好"},
    {"role": "assistant", "content": "是呀"},
    {"role": "user", "content": "要不去公园？"}
  ],
  "temperature": 0.6,
  "max_tokens": 1024
}
```

### 10.3 异常处理

| 情况 | 处理 |
|------|------|
| 豆包超时/失败 | 悬浮球红色闪烁，Toast "提取失败" |
| DeepSeek超时/失败 | 悬浮球红色闪烁，Toast "生成建议失败" |
| 返回非JSON | 从 content 提取 JSON（支持markdown包裹） |
| 两个Key都未配置 | 浮窗提示"请先配置API Key" |

---

> **文档结束。Chat-Helper v1.4 产品需求基线。**
>
> **v1.4 核心变更：**
> - 新增 5.2 悬浮球状态反馈（处理中→成功/失败 动画）
> - 新增 5.3 全局浮窗建议卡片（WindowManager，聊天App上方弹出，自动倒计时消失）
> - 重构 AI-2 Prompt：三重"说人话"禁止AI腔 + 输出重构为 direction + suggestions
> - 新增设置项：建议卡显示时间（5s/8s/15s/手动关闭）
> - suggestions 表新增 direction 字段
> - 交互流程重写：用户全程不需要切回 Chat-Helper App
